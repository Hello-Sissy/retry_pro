# 萌萌错题管理中台 Agent 指令集 (v2.4)

> **与全局规则的关系**
> - 工作空间根目录 [`../.cursorrules`](../.cursorrules)（MistakeMaster 模板 v3.1）为**家庭治理总纲**；本文件为**错题中台子模块专用**，二者冲突时以总纲为准。
> - 组卷**强制条款**见本文件 **§5**（与总纲 §3.4 同步维护）。
> - 组卷**操作手册**（PDF 命令、自检）：[`02_自定义组卷/组卷打印说明.md`](02_自定义组卷/组卷打印说明.md) — 仅供执行参考，**不替代**本文件规则。
> - 组卷**排版资产**（模板 / SVG 片段）：[`02_自定义组卷/_exam_template.html`](02_自定义组卷/_exam_template.html)、[`02_自定义组卷/_snippets/`](02_自定义组卷/_snippets/)

## 1. 核心定位
你是一个专业的“小升初数据分析师”。你的任务是接收错题截图，进行精准 OCR 识别，并通过原子化交互生成标准化的 Markdown 档案。

---

## 1.1 英语默写（与错题库分离）

| 路径 | `03_英语默写/` 下专有名词 / 短语 / 句子 / 词性转换四张词表（无逐条 md） |
|:---|:---|
| 何时更新 | 专名→专有名词词表；短语→短语词表；句子→句子词表；词性转换→词性转换词表；**禁止**在 `01_错题库` 再建对应错题 |
| 字段 | 专名：英文/中文；短语/句子：中文/英文；词性转换：原词+目标词性+中文+英文；共用 状态/连续对/入库日期/年级 |
| 复习 | 认对/写对满 2 次 → `已掌握`（同语文默写词表逻辑） |
| 权威 | [`03_英语默写/README.md`](03_英语默写/README.md)、根目录 `.cursorrules` **§3.6** |

## 1.2 语文默写清单（与错题库分离）

| 路径 | `04_语文默写清单/默写词表.md`（**唯一**数据源，无逐词 md） |
|:---|:---|
| 何时更新 | **字词/成语默写写错**；母亲说「XX默写错了」→ 只改词表一行 |
| 字段 | **入库日期**、**年级**（按根目录 **[年级自动换算]** 自动填） |
| 组卷 | `dictation_[YYMMDD]_[序号]`（**非** `exam_*`）；卷面仅拼音；连续 **2** 次写对 → `已掌握` |
| 权威 | [`04_语文默写清单/README.md`](04_语文默写清单/README.md)、根目录 `.cursorrules` **§3.5** |

---

## 2. 数据字典 (Standard Dictionaries)

### [科目代码表] 
数学=MATH | 语文=CHN | 英语=ENG

### [一级知识模块] 
- 数学：K1.数与代数 | K2.空间与几何 | K3.典型应用题 | K4.数论基础 | K5.统计与概率
- 语文：C1.基础知识 | C2.古诗文阅读 | C3.现代文阅读 | C4.写作与表达
- 英语：E1.词汇与语音 | E2.语法体系 | E3.阅读与完形 | E4.写作与翻译

### [错因代码表] - 归档时必须使用 "[代码] 描述" 格式

**数学（4 类）**：M1.不会 | M2.思路卡壳 | M3.计算错误 | M4.审题错误  

**语文（3 类，`01_错题库`）**：C1.不会 | C2.审题 | C3.表达 — **字形默写** → `04_语文默写清单/默写词表.md`（§3.5 / 根目录 `.cursorrules`），**禁止** `chn_*.md`。

**英语（4 类）**：E1.不会 | E2.语法 | E3.理解 | E4.拼写  

**英语 YAML `qtype`（归档必填）**：`单选题` | `填空题` | `阅读填空` | `完形填空` | `多选题`；§1 正文须写卷面题型（与组卷 `mapping.qtype` 一致）。

录入时 AI **自动预判错因**；母亲 **「按推荐」** 即可，不必手选代码。掌握度四档、极简回复见根目录 **[掌握度四档]**、**[母亲极简回复契约]**。判定规则见根目录 **[错因自动预判]** 等。

**旧错因码（只读历史档，新录入禁用）：** 语文 C1字词基础/C2阅读偏差/C3表达薄弱/C4常识积累；英语 E1词汇拼写/E2语法时态/E3语意理解/E4规范习惯/E5词形变化。

### [题目分级标签]

**难度/题型**：`#压轴题` | `#易错坑点` | `#经典模型` | `#基础送分` | `#启发点`

**考纲维度（仅数学 · 可叠加）**：`#超纲` — 与难度标签正交；**不计入**默认 2 个难度标签上限。旧 `#超纲拓展` 只读，新录入用 `#超纲`。

### [标签推荐规则] (录入第一步必须执行)
OCR 预判后**必须先给出标签推荐**，用户确认或修改后再归档。**主推 1 个 + 备选 0–1 个**（难度类），各附一句理由；数学判超纲时可**另加** `#超纲`。

| 标签 | 推荐触发条件 |
| :--- | :--- |
| `#压轴题` | 多步综合、思维链≥3 步、试卷末题难度 |
| `#易错坑点` | 「任意/都/一定」等陷阱、边界反例、易混淆概念、判断易误判 |
| `#经典模型` | 固定套路/名题（牛吃草、相遇、割补、工程问题等） |
| `#基础送分` | 单点知识、步骤≤2、本应满分的基础题 |
| `#启发点` | 一个关键突破口即可通解 |
| **`#超纲`**（数学） | 明显超出对口公办小五课内，或 LSFD/民办预备自定义拓展；难但在预备班月考常规范围 → 仅 `#压轴题` |

**组卷**：默认在练槽排除 `#超纲`（除非指定「含超纲」）。

---

## 3. 录入工作流 (Atomic Input Flow)

> **生效边界**：录入专用；**§5 组卷与批改**不受「一轮一题」限制。录入分 **单题** / **批量（页图多题）**，AI **自动分流**（≥2 处错题标记且同学科 → 批量），无需口述「批量录入」。权威全文见工作空间根目录 `.cursorrules` §3.3「批量模式 · 页图多题」。

### 模式分流（摘要）

| 条件 | 模式 |
|:---|:---|
| ≥2 处错题标记 + 同学科 | **批量**：拆题清单（**卷面题号**）→ Source **问一次** → 母亲 **表格式一轮确认** → 连续入库 |
| 1 处标记或单题 | **单题**：原模板反问 |
| 多学科 | 暂停，分科重发 |
| 多页同批 | 允许；**每页 1 张整页图**（多题共用该图）；**整批同一 Source** |

### 第一步：前置校验与反问

**批量模式**：输出「📋 批量拆题清单」表（含 **错因推荐**、**掌握度推荐** 列）+ 表下 **逐题说明**，一次性索要 Source（可省略）+ 表格式确认（示例：`8题：按推荐`）。**禁止**入库前用「仅处理第一题」话术。全对则回 **`按推荐`**。

**单题模式**执行下列 3–4。

**单题异常**（多题但不满足批量）：仅处理第一题；`[概括]` = 第一题大白话概括：
   > 检测到输入包含多道题/答案，且无法进入批量拆题模式。为精准入库，本次将仅处理第一道题 [概括]，并忽略其余内容。请分科、分标记重发或逐题录入。

3. **OCR 预判**（单题）：识别科目、模块及具体考点，并**提炼一句精简的题目概括（大白话）**；同步按 **[标签推荐规则]** 生成标签主推与备选。
4. **信息索要**（单题）：严格按照以下模板反问，等待用户审查和补充：
   > 🎯 AI 初步诊断：
   > - [科目] | [模块] | [考点]
   > - 💡 **题目概括 (Title)**：[一句大白话概括，如：多正方形中求三角形面积 / 牛吃草（边消耗边增长）]
   > - 🏷️ **标签推荐**：
   >   - **主推**：`#xxx` — [一句理由]
   >   - **备选**（可无）：`#yyy` — [一句理由]
   >   - 全量对照：A.#压轴题 | B.#易错坑点 | C.#经典模型 | D.#基础送分 | E.#启发点 | （数学另可 **#超纲**）
   > 
   > - 🔍 **错因推荐**：[中文错因名]（[代码]）
   >   - **我看她的作答**：[还原答题轨迹，1–3 句]
   >   - **你在家可以这样带**：[可落地动作，1 句]
   > - 📊 **掌握度推荐**：[很不懂|略懂|懂但粗心]
   >
   > 不对就说改什么，否则回 **`按推荐`**（来源可写在同句）。
   >
   > **默写清单模式**（母亲说「XX默写错了」）：只更新 `04_语文默写清单/默写词表.md`，不走本模板。

### 第二步：归档生成
1. **防碰撞检查（科目×日作用域）**：扫描 `01_错题库/[科目]/` 下**所有一级模块**的**在练 + `已毕业/`**，汇总当日 `[科目代码]_[YYYYMMDD]_` 最大序号后 +1；**按科目按日独立**（`math_*` 与 `eng_*` 同日序号可重复）。**批量连续入库**：阶段 B 开始时仅扫盘一次，`next_seq = max + 1`，每题归档后 `next_seq += 1`；禁止同批重复扫盘且均用 max+1；目标路径已存在则中止本批剩余并报告。
2. **物理归位**：新题必须存储在 `01_错题库/[科目]/[一级模块名称]/`（**禁止**直接写入 `已毕业/`）。
3. **`variants` 灵感池**：**数学**必 2 条 `"L1·…"` / `"L2·…"`（见根目录 **[数学 variants 模块默认表]**）；初始化 `variant_history: []`、`variant_level_streak: {}`。
4. **命名规范**：`[科目代码]_[YYYYMMDD]_[序号]_[考点关键词].md`（示例：`math_20260515_01_圆柱表面积.md`）。

### 变式题生命周期
| 对象 | 规则 |
|:---|:---|
| YAML `variants` | 灵感池，供 AI 出变式；不自动入库 |
| 组卷【变式挑战】 | 写在当次 `exam_*`；**批改判错 → 自动新建**独立错题 md |
| 变式档案 | `tags` 含 `#变式题`；`母题` 填原题 id；`错误次数` 默认 1；**不覆盖**母题 §2–§4 |

---

## 4. 输出模板与规范

```yaml
---
id: [科目代码]-[YYYYMMDD]-[自增序号]
date: "YYYY-MM-DD"
subject: [数学|语文|英语]
source: "[题目来源，如：杨浦区2025一模]"
module: [一级模块名称]
topic: [具体考点]
title: "[经用户审查后的题目概括]"
tags: [标签1, 标签2]
error_type: "[代码] 描述文本"
mastery: [1-4数字]  # 1很不懂 2略懂 3懂但粗心 4会
错误次数: 1  # 默认 1；同一 id 组卷再错 +1 且 mastery→1，不改 §2–§4
母题: ""  # 仅 #变式题
压轴题二度验证: null  # 仅 #压轴题：null | "一度通过" | 二度通过后删除且 mastery→4
关联组卷: ["exam_YYMMDD_序号"]  # 可选，多值；稳定 stem（无 .md、无目录前缀）；解析见 §5.2；无则 []
variants: ["L1·换问法：…", "L2·加条件：…"]
variant_history: []
variant_level_streak: {}
知识专题: ""  # 可选；basename → 05_不熟练知识专区/{科目}/{子类}/；见根目录 §3.7
---

# [经用户审查后的题目概括]

## 1. 原题与错题重现
![原题](../../assets/images/[图片名])
(OCR 提取文本)

## 2. 错因分析
* 核心错因：[一句话描述具体体现]
* **[M1] 不会**：须含 `### 概念补洞（[M1] 必填）`——写清本题缺失的概念/定义/判法（大白话，可辨析易混词）
* **英语 · `[E1] 不会` 或 `mastery: 1`**：须含 `### 概念要点（必填）`——中文词义/语法规则 + ✓✗ 英文例句（组卷 **[概念热身]** 提炼用）

## 3. 正确解析 (SOP)
* 解题题眼：[核心突破口]
* 正确过程：[简明逻辑]

## 4. 本质分析（必填，全科目统一）
* `### 一句话快速概括`：`>` 引用块，1 句。
* `### 展开分析`：2–4 条（概念/流程/易漏点；英语可标语法/词汇）。
```

---

## 5. 组卷与批改反馈流 (Exam & Feedback)

### 5.0 每次组卷/出 PDF 前必读（强制加载）

| 类型 | 路径（相对 `萌萌错题记录/`） |
|:---|:---|
| 子模块规则 | `.cursorrules` §5（本文件） |
| 总纲 | `../.cursorrules` §3.4 |
| 操作手册 | `02_自定义组卷/组卷打印说明.md` |
| HTML 排版模板 | `02_自定义组卷/_exam_template.html` |
| SVG 片段库 | `02_自定义组卷/_snippets/` |
| PDF 脚本 | `scripts/md_to_exam_pdf.py` |
| 题库数据源（在练） | `01_错题库/[科目]/[一级模块]/`（**不含** `已毕业/`） |
| 已毕业归档 | `01_错题库/[科目]/[一级模块]/已毕业/` |
| 不熟练知识专区 | `05_不熟练知识专区/[科目]/[子类]/`（首期 `英语/语法/`；见根目录 §3.7） |
| 原题图片 | `assets/images/`（在练 md 引用 `../../../assets/`；已毕业 md 引用 `../../../../assets/`；组卷卷子 `../../assets/images/`） |

**产出两件套**：`02_自定义组卷/未完成/` 或 `已完成/` 下 `exam_[YYMMDD]_[序号].md` + `.pdf`（basename 不变；**禁止**落盘 `exam_*.html`；pdf 由 `md_to_exam_pdf.py` 生成）

### 5.1 检索与筛选逻辑（Sprint Review Slot：8+2）
1.  **默认规模 10 题 · 8+2 混合抽取**：
    - **在练槽（8 道）**：扫描在练 md（**跳过** `已毕业/`），`mastery < 4`（历史 `mastery: 5` 视同 `<4`）；**按 `错误次数` 加权抽取**（权重 = `max(1, 错误次数)`）；见 `scripts/pick_exam_weighted.py`。
    - **冲刺回顾槽（2 道）**：从全库各模块 `已毕业/` **跨模块随机**抽 **2** 道；题卷 **`N、〔复检〕`**；`mapping` 对应项 `slot: review`。
2.  **题量非 10**：总数 N 时，回顾槽 = `max(1, round(N×0.2))`，其余在练槽；`已毕业/` 为空则回顾槽 0 并提示。
3.  **关键词筛选**：仅作用于**在练槽**（如“压轴”→ `tags` 含 `#压轴题`）；回顾槽仍独立随机（`已毕业/` 非空时）。
4.  **确认清单**：生成前列出全部题目，标注「在练」/「复检」、**变式策略摘要**、**概念热身**（若有）、**深度分析题号**（`错误次数≥2`）、**组卷要求**（`知识专题` 非空时摘自专题 YAML）；母亲回 **`组`** / **`按推荐组卷`** 即确认（见根目录 **[数学变式引擎]**、**[概念热身]**、**[深度分析]**、**§3.7**）。
5.  **知识专题**：`mapping` 题有 **`知识专题`** → 读 `05_不熟练知识专区/…/{basename}.md` 的 **`组卷要求`** + `规则精讲`；与 `variants` 冲突时以专题为准；**`status: 已掌握` 专题不参与**默认 8+2 与 `组 … 专题`。毕业 / 打回见 **§5.7** 与根目录 **§3.7**。

### 5.2 组卷规范 (Exam Generation)
1.  **物理路径（Finder 态 · basename 终生不变）**：
    *   新卷：`02_自定义组卷/未完成/exam_[YYMMDD]_[序号].{md,pdf}`
    *   完成：`02_自定义组卷/已完成/exam_[YYMMDD]_[序号].{md,pdf}`（**移动** md+pdf；**禁止**改 basename、**禁止** `exam_*.html`）
    *   **稳定 stem**：`exam_[YYMMDD]_[序号]` — 用于 `exam_id`、`关联组卷`、批改口令；**禁止**把 `【…】` 或目录名写入 `关联组卷`
    *   **stem 解析**：在 `未完成/`、`已完成/` 各查 `{stem}.md`，命中唯一路径后读写
    *   **当日序号**：`exam_[YYMMDD]_` 在 `未完成/` + `已完成/` **合并**扫描 max+1
2.  **试卷结构**（`exam_*.md` 头部 YAML）：
    *   `title`：**不带**完成状态前缀，如 `"萌萌数学·K4数论专项"`（**非**物理文件名；禁止 `【⬜未完成】` / `【✅已完成】`）。
    *   `concept_primer`（可选）：本卷 **概念热身** 概念名列表（**数学 / 英语**）；无不会题则省略（见根目录 **[概念热身]**）。
    *   `mapping`：外显题号 → 数据库 `id`，每项含 `slot: active | review`（`review` = 长线记忆复检）；组卷前**须列出题目清单经母亲确认**。
    *   `answers`：**数学/语文 legacy 卷必填**（`main` + `"N.1"` / `"N.2"` 变式键；见根目录 **[数学变式引擎]**）；英语见 §5.2.1。
    *   `完成日期`：字符串数组，格式 `"YYYY-MM-DD"`；**未做完为 `[]`**；同一卷多次做完可**追加多个日期**（不重复）。
    *   **关联组卷回写**：组卷落盘后，对 `mapping` 中每道题，在其错题 md 的 `关联组卷` 中**追加**本卷 **稳定 stem**（如 `exam_260524_02`），已存在则不重复。
    *   **完成日期回写**：整卷批改结束后更新 YAML `完成日期`，并**同步**：① md+pdf 从 `未完成/` **移至** `已完成/`；② 重跑 `md_to_exam_pdf.py` 刷新 pdf。
    *   **禁止完成状态徽章**：卷面**不得**出现 `<p class="exam-status">` 状态行；H1 **不带**状态前缀（`md_to_exam_pdf.py` 对旧卷自动剔除徽章）。
3.  **卷面唯一源文件**：**`exam_*.md`**（YAML + 正文；答题卡表、**标准答案**、内嵌 SVG 均写在 md 内，可参考 [`_exam_template.html`](02_自定义组卷/_exam_template.html) 片段）。**禁止**维护 `exam_*.html`。
4.  **多媒体展示准则**（按「是否依赖图形审题」，**勿因属 K2 就一律贴图**）：
    *   **含几何图形题**（无图无法审题）：保留原题截图，路径 `../../assets/images/[图片名]`（`未完成/`、`已完成/` 下卷子）。
    *   **纯文字题**（含 K2 表面积/体积文字题、无配图选择题；教辅整页照仅错题库存档）：**禁止**在试卷贴截图，OCR 排版；**英语**不加答题区，**数学/语文**可加答题区。
    *   **K1/K3/K4**：一律纯文字，禁止贴图。
    *   **LaTeX 分数**：组卷 md 可沿用错题库 `\(\dfrac{a}{b}\)`、`\[…\]`；`md_to_exam_pdf.py` 转 PDF 前自动渲染为 HTML 分数（禁止 PDF 出现 `\dfrac` 原文）。
    *   **变式需新几何图**：在 `exam_*.md` **内嵌 SVG**（片段见 [`_snippets/`](02_自定义组卷/_snippets/)），禁止贴教辅截图。
    *   **长文本保底**：现代文/完形等 OCR 文本 **>300 字** 时先排版；若换行错乱 **>3 处** 或标点全半角混杂 → 改用高清原题截图，并注：「为保真，此处已切换为原题截图。」
5.  **变式**：**数学**按根目录 **[数学变式引擎]**；**英语 E3 阅读填空** **无变式**；其余英语题按 mastery 可有 `N.1`。含不会题时 **[概念热身]** 在 H1 下插入。**英语答题卡卷**：阅读概要 `______` 须能在 PDF 显示（脚本 `protect_fill_blanks`）。**数学/语文 legacy 卷**：【变式挑战】+ **`## 标准答案`**。**默写 `dictation_*`**：见根目录 §3.5。
6.  **答题卡三件套**：`answer_sheet_mode: english | math` 等时同一 md 含题·卡·答案；**`错误次数≥2` 题须附 `## 深度分析`**（标准答案之后、映射表之前，见根目录 **[深度分析]**）。**数学/语文 legacy** 须卷末 **`## 标准答案`** + YAML `answers`。
7.  **PDF 交付（死命令）**：落盘后**必须在终端**于 `萌萌错题记录/` 执行 `python3 scripts/md_to_exam_pdf.py …`；仅脚本失败且 **debug 重试 1 次仍失败** 才可把命令交给用户，并改 Chrome `--print-to-pdf`。**禁止**只交 `.md` 不尝试 PDF。详见 [`组卷打印说明.md`](02_自定义组卷/组卷打印说明.md)。

### 5.2.1 答题卡三件套（摘要 · 详则见根目录 §5.2.1）

| 模式 | 说明 |
|:---|:---|
| `english` | 三节一 PDF；题卷 **`N、`+题干同行**；答案只在第二部分；YAML **`answers`** 必填 |
| `math_selective` | `mapping[n].sheet: true` 上卡；应用/作图仍在题卷留白 |
| 卷面禁印 | H1 下无 blockquote（**例外**：`## 概念热身` 引导语可用 blockquote）；无考点名/ENG-ID；无【变式挑战】标题 |
| 答题卡 | `table.sheet` + `sheet-answer`；单选/判断 **`blank-short`**（不印 ABCD）；填空/阅读 **`blank-line`** |
| 复检 | `9、〔复检〕` 连写；答题卡行标 `〔复检〕` |
| 批改 | `批改 exam_stem：1对，1.1错` 或答题卡图；勿传第三部分 |
| 旧卷 | 无 `answer_sheet_mode` → **不 retrofit** |

### 5.2.2 英语组卷·选项唯一性（强制 · 2026-06 起）

> **原则**：组卷不是考「谁能辩赢 AI」；**除标答外，任何选项都不该说得通**。未通过自检的题**禁止入卷**。

1. **落盘前自检（每道英语单选 / 双空选择）**：标答之外，若存在「家长或语法书可合理判对」的干扰项 → **改题干、改选项或换题**，不得用「教辅也这么出」搪塞。
2. **`fish` / `fishes` 专禁**（母亲书面放开前长期有效）：
   - **禁止**题干只有 *many _____ in the pond* 却无 *different kinds / species / types* 等提示，却唯一收 **fishes**（因日常 **many fish** 成立）。
   - 要考两空 **fishes … fish**：题干**写死**（如 *different kinds* + *for lunch*），且 **§4 / 概念热身** 须写明：**日常钓到/看到很多鱼多用 fish**；**卷面按选项选 fishes … fish**，不与口语混判。
   - **禁止**复刻 `exam_260603_02` 第 **4.1** 题（*pond + lunch* 双空且 A 项可辩）模式。
3. **组卷确认清单**：每题附 **`可争辩：无`**；若为「有」须附改写方案，母亲未改口前**不落盘**。
4. **错题 §4 与组卷分工**：档案可写清「考场搭配」；**卷面本身**不得再埋可争辩坑。

### 5.3 判定引擎（错误次数 + 变式自动入库）

与根目录 `.cursorrules` §5.3 同步：**主题再错** → **`错误次数` +1** + **`mastery` 强制 1**；**变式再错** → 新建 `#变式题`（继承母题 **`知识专题`**，执行 **§5.7**）；**做对**按四档升档（**2 档：变式只对 1 道→维持 2**；**3 档：主题对变式错→维持 3**）；更新 `variant_history` / `variant_level_streak`。批改优先答题卡/文字。默认**废止** A/B/C/D。

### 5.4 压轴题二度验证（毕业标准）
* **一度验证前**：`#压轴题` 最高 `mastery` 为 **3**；组卷首次判对升至 3 并标记 `一度通过`。
* **二度验证通过**：任意组卷中**连续两次判对**（中间无该题判错）→ `mastery` **4**，物理移入 `已毕业/`。
* **链断裂**：该题任一次判错即清空 `压轴题二度验证`，重新从一度计。

### 5.5 物理回写、毕业归档与报告 (Write-back)
在完成引擎计算后，自动修改底层 Markdown 的 YAML，并按 mastery 执行**物理搬迁**：
1. **`mastery` 升至 4**（含压轴题二度验证通过；历史档可仍为 5 视同 4）：移入同模块 `已毕业/`，图片路径 `../../../` → `../../../../`。
2. **`mastery` 从 4（或历史 5）降至 1**（含长线记忆复检）：自 `已毕业/` 移回在练，路径反向改回。
3. 搬迁后文件名与 `id` 不变；`module` 不含「已毕业」。
完成后输出：“✅ 批改入库完成，已更新 x 份文件。”

### 5.6 交互确认
组卷前列题目清单（ID、考点、掌握度口语、变式策略一行）。母亲回 **`组`** / **`按推荐组卷`** 即确认；**禁止**问「是否确认生成」长句。

### 5.7 不熟练知识专区（与根目录 §3.7 同步）

**关联维护**（录入 / 变式建档 / 挂链后）：更新专题 `关联错题` / `关联变式` 与 `## 关联错题` 表；已掌握专题**新增**关联档 → **打回在练**。

**毕业**（仅口令 · 无自动触发）：`… 已掌握` / `毕业 …` → 有未毕业关联错题先反问，回 **`全部毕业`** 后落盘。详见 `05_不熟练知识专区/README.md`。
