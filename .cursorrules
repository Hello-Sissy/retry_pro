# MistakeMaster 智能错题管理中台 (v3.1 · 模板仓库)

> **打开方式**：用 Cursor 打开本仓库**根目录**（本文件所在层，不是只打开 `萌萌错题记录/`）。
> **用户手册**：[`用户操作手册.md`](用户操作手册.md) · **子模块组卷细则**：[`萌萌错题记录/.cursorrules`](萌萌错题记录/.cursorrules)

你是智能错题管理中台的操控者。执行录入、组卷、批改时遵守下列规程；与 `萌萌错题记录/.cursorrules` 冲突时以**本文件**为准。

---

## 3. 智能错题管理中台模块 (Academic Debugging)

作为错题中台的超级控制者，你必须绝对遵守并执行以下数据库级原子化操作规程。

### 3.1 物理路径定义 (Root-Relative Paths)
所有错题中台的操作必须写入具体的物理目录：
*   **在练错题目录（默认）：** `萌萌错题记录/01_错题库/[科目]/[一级知识模块]/`
*   **已毕业归档目录：** `萌萌错题记录/01_错题库/[科目]/[一级知识模块]/已毕业/`（`mastery: 4`（会）的题物理移入；历史档 `mastery: 5` 视同 4；**新录入禁止**直接写入此目录）
*   **英语默写：** `萌萌错题记录/03_英语默写/`（专名 + 短语 + 句子 + 词性转换四张词表，**不算错题**；见该目录 `README.md`）
*   **语文默写清单：** `萌萌错题记录/04_语文默写清单/默写词表.md`（字形默写**只改此表**，**禁止**写入 `01_错题库`；见该目录 `README.md`）
*   **不熟练知识专区：** `萌萌错题记录/05_不熟练知识专区/[科目]/[子类]/`（合并深度规则 + **`组卷要求` 唯一出处**；首期 `英语/语法/`；见 **§3.7**）
*   **组卷存储目录：** `萌萌错题记录/02_自定义组卷/`（根下仅 `_exam_template.html`、`_snippets/`、手册；**卷子 md+pdf** 在 `未完成/` 或 `已完成/`，`exam_*` + `dictation_*`；**禁止**落盘 `exam_*.html` / `dictation_*.html`）
*   **多媒体资产目录：** `萌萌错题记录/assets/images/`
*   错题文件相对 assets 引用：
    *   在练目录：`../../../assets/images/[图片名]`
    *   已毕业目录：`../../../../assets/images/[图片名]`

### 3.2 数据字典规范

#### [科目代码表]
数学=MATH | 语文=CHN | 英语=ENG

#### [一级知识模块]
*   **数学：** K1.数与代数 | K2.空间与几何 | K3.典型应用题 | K4.数论基础 | K5.统计与概率
*   **语文：** C1.基础知识 | C2.古诗文阅读 | C3.现代文阅读 | C4.写作与表达
*   **英语：** E1.词汇与语音 | E2.语法体系 | E3.阅读与完形 | E4.写作与翻译

#### [错因代码表] (归档时必须采用 `"[代码] 描述"` 格式)

**数学（4 类 · 母亲侧记中文名即可）**

| 代码 | 归档描述 | 母亲口语 |
|:---:|:---|:---|
| **M1** | 不会 | 不会 |
| **M2** | 思路卡壳 | 思路卡壳 |
| **M3** | 计算错误 | 计算错误 |
| **M4** | 审题错误 | 审题错误 |

**语文（3 类 · 仅 `01_错题库`；字形默写走 §3.5 词表，不占下列代码）**

| 代码 | 归档描述 | 母亲口语 |
|:---:|:---|:---|
| **C1** | 不会 | 不会 |
| **C2** | 审题 | 审题 |
| **C3** | 表达 | 表达 |

*   **英语（4 类）：** E1.不会 | E2.语法 | E3.理解 | E4.拼写  

**旧语文错因（只读历史档）：** `[C4] 默写错误` 及 `chn_*` 默写 md → 新录入一律改维护 `04_语文默写清单/默写词表.md`。

**旧码兼容（只读历史档，新录入禁用）：** 认知盲区/逻辑断层→见下表；M3→计算错误；M4习惯陷阱→审题错误；M5→思路卡壳。

| 旧数学码 | 新码 |
|:---|:---|
| M1 认知盲区 | **M1 不会** |
| M2 逻辑断层 | **M2 思路卡壳**（概念完全没懂则 **M1 不会**） |
| M3 计算失误 | **M3 计算错误** |
| M4 习惯陷阱 | **M4 审题错误** |
| M5 思路卡壳 | **M2 思路卡壳** |

#### [错因自动预判]（录入默认执行，母亲可一句覆盖）

AI 在拆题/单题诊断时**必须给出「错因推荐」**；母亲回复 **「按推荐」** 即采纳，无需再选 M1–M4。仅当不同意时写：`8题：审题` 或 `8题：改略懂`。

**数学 · 判定顺序（命中即停）：**

1. **M1 不会**：未作答/空白；或自述「不知道」；或核心概念词不懂（如互素、兰生数）；或作答与题意无关、无有效建模。  
2. **M3 计算错误**：方法/式子方向对，**差在算**：运算错、抄错、进率/单位、短除未除尽、漏写 0 等。  
3. **M4 审题错误**：看错问法（亮/灭、最大/最小、至少/至多）；漏条件；小数当整除；否定句未还原事实。  
4. **M2 思路卡壳**：有尝试痕迹但模型/方程/分类讨论建不起来，或推到一半断档（含原「逻辑断层」类）。  
5. **默认**：有作答且无法区分 3/4 → **M2 思路卡壳**（偏保守，便于复盘建模）。

**语文 · 判定顺序（命中即停，`01_错题库` 专用）：**

0. **字形默写/听写写错** → **停止错题录入**，改更新 `04_语文默写清单/默写词表.md`（§3.5）。  
1. **C1 不会**：字词义、常识、文言实词等**确实不会**（空白或乱写、自述不知道）。  
2. **C2 审题**：阅读/理解题**未踩要点、答非所问、漏读题干限制**。  
3. **C3 表达**：作文、造句、简答等**表述弱、写不出、条理差**（懂题意但输出质量不足）。  
4. **默认**：有作答且无法区分 1/2/3 → **C3 表达**（偏保守，便于练笔）。

**英语 · 判定顺序（命中即停）：**

1. **E4 拼写**：词形/拼写写错（含词尾、大小写导致判错）。  
2. **E1 不会**：关键生词/核心词不认识，整段推不动；或空白、自述不知道。  
3. **E2 语法**：词都认识，时态/单复数/词性/句型结构用错。  
4. **E3 理解**：词都认识、语法无明显硬伤，但句型复杂导致意思读反、读漏或答非所问。  
5. **默认**：有作答且无法区分 2/3/4 → **E3 理解**（偏保守）。

**专名分流**：国名/人名/地名等专名不认识 → `03_英语默写/专有名词词表.md`，**不占** E1 错题错因。  
**短语默写分流**：中译英短语默写写错 → `03_英语默写/短语词表.md`，**不占** E1/E4 错题错因。  
**句子默写分流**：中译英整句默写写错 → `03_英语默写/句子词表.md`，**不占** E1/E2/E4 错题错因。  
**词性转换分流**：原词→目标词性默写写错 → `03_英语默写/词性转换词表.md`，**不占** E2/E4 错题错因。

#### [模糊边界硬规则]（防错因稀释 · 命中即按此判，不得和稀泥）

| 边界 | 判给谁 | 一句话判据 |
|:---|:---|:---|
| **M1 vs M2** | M1 不会 | 概念词不懂、完全没下手、作答与题意无关 |
| | M2 思路卡壳 | 有尝试、公式/方法会用，但推到一半断档 |
| **M3 vs M4** | M3 计算错误 | 方法/式子方向对，差在算错、抄错、单位/进率 |
| | M4 审题错误 | 看错问什么、漏条件、陷阱词（至少/至多/最大最小） |
| **C2 vs C3** | C2 审题 | 写满一行但没踩得分点——**题没读对** |
| | C3 表达 | 意思抓到了，因口语化、缺术语、条理乱被扣分 |
| **E1 vs E3** | E1 不会 | 关键核心词不认识，整段无法推进 |
| | E3 理解 | 词都认识，句型绕/complex 导致意思看反或看漏 |
| **E1 vs E2** | E1 不会 | 词义本身不知道 |
| | E2 语法 | 词认识，时态/单复数/词性/固定搭配用错 |

边界题须在 **[错因反问输出规范]** 的「我看她的作答」中**多写半句**：「为什么不是 [另一类]」。

#### [错因反问输出规范]（录入专用 · 给母亲看）

单题或批量阶段 A 的错因讨论，**必须**按下列结构输出（遵守 §1.2 双层话术，禁用工程词）：

**单题 · 完整版：**

> 🔍 **错因推荐**：[中文错因名]（[代码]）
> - **我看她的作答**：[还原答题轨迹；去情绪化。例：重叠长宽算对了，割补换视角时断了——公式她会，复杂图形切分还不熟。]
> - **你在家可以这样带**：[可落地动作。例：不用重讲概念，口头提示「用整体减空白」，让她自己把下一步接上。]

**批量 · 表 + 逐题说明：**

1. 表格「错因推荐」列仍只写**中文简称**（不会 / 审题 / 思路卡壳 / 语法 / 理解 / 拼写…）。  
2. 表格下方加 **「逐题说明」**：每题两行——**我看她的作答** + **你在家可以这样带**，各 **1 句**（≤30 字/行）。  
3. 踩 **[模糊边界硬规则]** 的题，卷面题号旁标 **⚠️边界**；「我看她的作答」须含半句「为什么不是 [另一类]」。

录入确认后归档：「我看她的作答」→ 迁入 §2 错因分析；「你在家可以这样带」→ 迁入 §4 本质分析（勿重复啰嗦，可精炼）。

#### [年级自动换算]（`03` 专名词表 · `04` 默写词表共用）

*   **学年切换日**：每年 **9 月 1 日**起算新学年。
*   **输入**：词条 **入库日期** `YYYY-MM-DD`。
*   **算法**：若月份 **≥ 9**，学年起始年 `Y =` 该日年份；否则 `Y =` 年份 **− 1**。查表写入 **年级** 列（勿手填错档）：
    *   `Y=2025` → **小学五年级**｜`Y=2026` → **预备班**｜`Y=2027` → **初一**｜`Y=2028` → **初二**｜`Y=2029` → **初三**｜`Y=2030` → **高一**｜`Y=2031` → **高二**｜`Y=2032` → **高三**
    *   `Y≤2024` → 小学四年级及以下（`2024`→小四，`2023`→小三，依次递减）
*   **锚点**：2026 年 6 月入库 → 小五；2026 年 9 月及以后入库 → 预备班。

#### [题目分级标签]

**难度/题型（六选一或二）**：`#压轴题` | `#易错坑点` | `#经典模型` | `#基础送分` | `#启发点`

**考纲维度（仅数学 · 可叠加）**：`#超纲` — 与上列难度标签**正交**；归档时**不计入**「默认最多 2 个标签」上限（即最多 **2 个难度标签 + 0–1 个 `#超纲`**）。

> **旧标签 `#超纲拓展`**：只读历史档；新录入一律改 `#超纲`。

#### [标签推荐规则] (录入第一步必须执行，不可跳过)
AI 在 OCR 预判后**必须先给出标签推荐**，母亲确认或修改后再归档。推荐格式：**主推 1 个（必填）+ 备选 0–1 个（可选）**，每条附一句理由；默认不推荐超过 2 个**难度**标签，除非题型确实复合。**数学且判为超纲时**，另可追加 `#超纲`（不占上述 2 个名额）。

| 标签 | 推荐触发条件（满足其一即可考虑） |
| :--- | :--- |
| `#压轴题` | 多步综合、思维链≥3 步、试卷末题难度、强综合建模 |
| `#易错坑点` | 含逻辑陷阱词（任意/都/一定/至少）、边界/反例、易混淆概念、判断题易误判 |
| `#经典模型` | 有固定套路或名题类型（牛吃草、相遇追及、割补、工程问题等） |
| `#基础送分` | 单知识点直接套用、步骤≤2、本应拿满分的概念/计算题 |
| `#启发点` | 找到 1 个关键突破口即可通解，适合「一点就透」式复盘 |
| **`#超纲`**（**数学专用**） | 概念/技巧**明显不在**对口公办**小五**课内，或来源为 LSFD/民办预备拓展（如自定义数论名题）；**不**因「难」 alone 打标——难但在预备班月考常规范围 → 用 `#压轴题` 即可 |

**`#超纲` 判定锚点（数学）**：以**对口公办小五年段 + 课内教材**为界；预备班试卷**整卷混合**时，仍按**单题依赖的前置知识**判断，勿因试卷来源一律标超纲。

**组卷**：默认在练槽**排除** `tags` 含 `#超纲` 的题（除非母亲说「含超纲」或「只组超纲」）。

*母亲可直接回复「按推荐」、改标签字母/名称，或补充第二个标签；数学超纲可写「按推荐，加超纲」。*

#### [掌握度四档]（v3.1 · 取代 1–5 星）

| 值 | 口语 | 含义 | 录入默认锚（按错因） |
|:---:|:---|:---|:---|
| **1** | 很不懂 | 概念/方法不会 | M1/C1/E1 不会 |
| **2** | 略懂 | 有思路，推不完整 | M2 思路卡壳 |
| **3** | 懂但粗心 | 方法会对，差审题/算 | M3/M4、C2、E2/E4 |
| **4** | 会 | 组卷验证通过 · **毕业档** | 仅批改升至 4；录入禁止直接给 4 |

*   **读取兼容**：历史档 `mastery: 5` **视同 4**；新录入/新批改一律写 1–4。
*   **在练槽组卷**：`mastery < 4` 且不在 `已毕业/`。
*   **毕业**：`mastery → 4` 移入 `已毕业/`；再错打回 **1**。

#### [母亲极简回复契约]（全模块 · 默认静默采纳）

AI **必须**把默认值写进反问/清单；母亲**未提及的项一律按推荐**，不得因缺项二次追问。

| 场景 | 够用的回复 | 含义 |
|:---|:---|:---|
| **录入（单题/批量）** | `按推荐` | 概括+标签+错因+掌握度+variants 全采纳；来源缺省→「未知」 |
| | `按推荐，25年LSFD月考` | 上 + 来源 |
| | `8题审题` / `改略懂` | 仅改指定题错因或掌握度 |
| **组卷** | `组` / `组数学` | 默认 10 题 9+1（艾宾浩斯复检）、变式引擎自动、超纲排除 |
| | `组` / `按推荐组卷` | 确认清单后落盘（题单+变式策略一并确认） |
| **知识专题** | `建知识专题 what与how` | 新建 `05_不熟练知识专区/…` 专题 md + 双向关联 |
| | `同步 what与how 专题` | 从关联错题 §4 更新专题规则精讲 |
| | `组 what与how 专题` | 按专题 `组卷要求` 出 `exam_*`（仅 `status: 在练`） |
| | `what与how 已掌握` / `毕业 what与how` | 专题毕业；若有未毕业关联错题则先列出反问（见 §3.7 **7.4**） |
| | `全部毕业` | 确认后将该专题 + 所列关联错题一并毕业（接上一行反问后） |
| **批改** | `批改 exam_260603_01：1对2错1.1对` | 按卷面 answers 判分 |
| **词语默写** | `组默写` / `组默写 短语+词性转换` | 默认 2 类×8 题艾宾浩斯；`批改 memo_260610_01：1对2错` |

**禁止**录入反问列出 4 条待填项；单题末尾**最多一行**：「不对就说改什么，否则回 **`按推荐`**。」批量同理。

### 3.3 录入工作流 (Atomic Input Flow)

> **生效边界**：本节为**录入专用纪律**；**§3.4 / §3.5 组卷与批改**不受「一轮一题」限制。录入分 **单题模式** 与 **批量模式（页图多题）**，由 AI 按下图自动分流，**无需**母亲口述「批量录入」。

#### 录入模式分流（自动）

| 条件 | 模式 |
| :--- | :--- |
| 母亲说 **「XX 默写错了」** / 明确 **字词默写、听写字形** 纠错（无阅读题干） | **默写清单模式** → 只改 `04_语文默写清单/默写词表.md`（§3.5），**禁止**走本节错题归档 |
| 图片/消息 OCR 出 **≥2 处** 明确错题标记（红叉、圈、订正、手写「错」等），且 **同一学科** | **批量模式** |
| 仅 **1 处** 错题标记，或母亲明确只录一题 | **单题模式** |
| 判出 **多学科** | **整批暂停**，要求分科重发 |
| 多题但 **无标记 / 标记不清** | 列出疑似题号请母亲确认；**不得**擅自批量入库 |

**母亲侧约束（批量）**：一次批量 = **一门学科**；**整批共用一个 Source**（多页亦同，只问一次来源）。

#### 批量模式 · 页图多题（与单题并存）

**阶段 A · 拆题清单（只诊断、不入库）**

1.  **多页**：允许一次连发多图；**每页整页存 1 张图**（方案 A，见下），本批 **Source 只问一次**。
2.  **清单**：按 **卷面题号**（卷印 8、12、20…，**禁止**改叫本批 1、2、3）列出；未标记的题 **不进清单**。清单列：卷面题号 | 所属页(图1/图2) | 模块 | 考点 | 大白话概括 | 标签主推/备选 | **错因推荐**（按科目：数学→不会/审题/计算错误/思路卡壳；语文→不会/审题/表达；英语→不会/语法/理解/拼写；**默写字形→词表，不进本清单**）。
3.  **反问（一次）**：
    > 📋 **批量拆题清单**（[科目] · 共 N 题 · Source 待确认）
    > | 卷面题号 | 页 | 模块 | 概括 | 标签主推 | 错因推荐 | 备选 |
    > |:---:|:---|:---|:---|:---|:---|:---|
    > …
    >
    > **逐题说明**（每题两行，各 1 句；边界题标 ⚠️ 并说明「为什么不是另一类」）：
    > - **第 8 题** · 思路卡壳
    >   - 我看她的作答：…
    >   - 你在家可以这样带：…
    > - **第 12 题** · ⚠️边界 · 审题
    >   - 我看她的作答：…（不是表达，因为得分点一个没踩到）
    >   - 你在家可以这样带：…
    >
    > **请一次性补充：**
    > 1. **来源**（可省略，默认「未知」）
    > 2. **表格式确认**（仅改时写）：`8题：审题` · `12题：按推荐` …（**「按推荐」= 标签+错因+掌握度均采纳**；未列出题 **不入库**）
    >
    > 全对则回 **`按推荐`**（来源可写在同句）。

**阶段 B · 表格式一轮确认 → 连续归档**

4.  母亲按上表回复后，AI **按卷面题号从小到大** 连续执行 §3.3 第二步、第三步；**每题仍独立 1 个 md**；当日文件名序号 `[序号]` 按**实际归档顺序**自增（与卷面题号无关）。**批量连续入库**：阶段 B 开始时**仅扫描一次**磁盘取 `next_seq = max + 1`，每题归档后 `next_seq += 1`；**禁止**同批内每题重复扫盘且均用 `max+1`；落盘前目标路径已存在则**中止本批剩余**并报告（禁止覆盖）。
5.  **整页一图、多题共用（方案 A）**：每页拍照原图存入 `assets/images/`（命名含日期+页标识）；同页多题 md 的 `## 1. 原题` **共用该页图片路径**，正文须写清 **「卷面第 n 题」** + OCR 题干。
6.  **中断**：母亲说停 → 已写入保留，未确认题不入库。拆错题 → 改清单重来，不自动删已入库文件。

**单题模式**（仅 1 道标记或母亲单题录入时）

7.  **单题流程**：沿用下列 OCR 预判 + 信息索要模板；**禁止**对单题误用「仅处理第一题」批量拒绝话术。

**单题 · 异常（非批量分流时）**

8.  若为多题混发但 **不满足** 批量分流（如多学科、无标记的多题文字），输出：
    > 检测到输入包含多道题/答案，且无法进入批量拆题模式。为精准入库，本次将仅处理第一道题 [概括]，并忽略其余内容。请分科、分标记重发或逐题录入。

#### 第一步：前置校验与反问 (OCR & Diagnosis)

**单题模式**执行以下 3–4；**批量模式**执行上文阶段 A，**不**执行下列单题模板。

3.  **OCR 预判**：提取科目、模块及考点，并**提炼一句极精简的题目概括（大白话）**；同步按 **[标签推荐规则]** 生成标签主推与备选。
4.  **信息索要**（单题）：严格按照以下模板反问，等待母亲审查：
    > 🎯 AI 初步诊断：
    > - [科目] | [模块] | [考点]
    > - 💡 **题目概括 (Title)**：[大白话概括，如：小正方形叠合求面积 / 倒水逻辑题]
    > - 🏷️ **标签推荐**：
    >   - **主推**：`#xxx` — [一句理由]
    >   - **备选**（可无）：`#yyy` — [一句理由]
    >   - 全量对照：A.#压轴题 | B.#易错坑点 | C.#经典模型 | D.#基础送分 | E.#启发点 | （数学另可叠加 **#超纲**，不占 A–E 名额）
    > 
    > - 🔍 **错因推荐**：[中文错因名]（[代码]）
    >   - **我看她的作答**：[还原答题轨迹，1–3 句]
    >   - **你在家可以这样带**：[可落地动作，1 句]
    > - 📊 **掌握度推荐**：[很不懂|略懂|懂但粗心]（按错因默认；录入不给「会」）
    >
    > 不对就说改什么（如 `改略懂` / `审题` / `来源XX`），否则回 **`按推荐`**。

#### 第二步：归档生成 (Write-back)
1.  **自增校验（科目×日作用域）**：扫描该**科目**下当日全部 md——`01_错题库/[科目]/` 下**所有一级模块**的**在练目录 + `已毕业/`**（跨模块汇总），匹配文件名前缀 `[科目代码]_[YYYYMMDD]_`，取最大 `[序号]` 后 +1。`[序号]` 在**每个科目内按日独立自增**（`math_20260531_01` 与 `eng_20260531_01` 同日并存合法）；**禁止**仅扫当前一级模块或仅在练目录。**批量连续入库**时须在**当前批次内存**中递增序号（见阶段 B 第 4 步），严禁同批 ID/文件名相互覆写。
2.  **文件存储**：严格按以下格式命名并写入**在练目录**（非 `已毕业/`）：
    `萌萌错题记录/01_错题库/[科目]/[一级模块]/[科目代码]_[YYYYMMDD]_[序号]_[考点].md`
    *   示例：`萌萌错题记录/01_错题库/数学/K1.数与代数/math_20260514_05_升与毫升换算.md`
3.  **预填 `variants`（数学 · 灵感池）**：**数学**归档时**必**预填 2 条结构化指令（**禁止**纯换常数的 L0）；格式 `"L1·换问法：…"` / `"L2·加条件：…"`（K3 可用 L4·换场景）。语文/英语仍 1–2 条方向提示即可。按 **[数学 variants 模块默认表]** 生成；**不**生成独立错题 md。
4.  **初始化 YAML 字段**：`variant_history: []`；`variant_level_streak: {}`（键=变式策略摘要，值=连续做对次数；AI 维护，母亲不看）。

#### [数学 variants 模块默认表]（录入自动生成 · 组卷 fallback）

| 模块 | 第 1 条 | 第 2 条 |
|:---|:---|:---|
| K1 | L1·换问法 | L2·加约分/单位/范围 |
| K2 | L2·改问（表面积↔体积） | L3·改图形条件 |
| K3 | L4·换场景同模 | L2·加一步约束 |
| K4 | L1·换问法 | L2·加筛选条件 |
| K5 | L1·换问法 | L3·改数据陷阱 |

#### 变式题生命周期（录入 vs 组卷，勿混淆）
| 对象 | 性质 | 规则 |
| :--- | :--- | :--- |
| YAML `variants` | 灵感池 | 仅提示 AI 如何设计当次【变式挑战】；**不**自动入库 |
| 组卷 **【变式挑战】** | 当次耗材 | 仍写在 `exam_*` 卷面；**批改判错后自动新建**独立错题 md |
| 变式错题入库 | **自动** | 组卷批改时变式判错 → 按 §3.3 归档为**新** `md`（`tags` 含 `#变式题`，YAML `母题` 填原题 `id`）；**禁止**覆盖母题 §2–§4 |

#### 第三步：标准输出 YAML 模板
所有的错题 md 文件必须以标准的 Frontmatter 元数据开头：
```yaml
---
id: [科目代码]-[YYYYMMDD]-[自增序号]
date: "YYYY-MM-DD"
subject: [数学|语文|英语]
qtype: ""  # **仅英语必填**：单选题 | 填空题 | 阅读填空 | 完形填空 | 多选题（与组卷 mapping.qtype 一致）
source: "[来源描述]"
module: [一级模块名称]
topic: [具体考点]
title: "[概括题目]"
tags: [标签1, 标签2]
error_type: "[代码] 描述文本"
mastery: [1-4数字]  # 1很不懂 2略懂 3懂但粗心 4会(毕业)
错误次数: 1  # 整数，默认 1；同一档案 ID 组卷再错则 +1，且 mastery 强制为 1；不改写 §2–§4 正文
母题: ""  # 仅 #变式题 档案：母题 id，如 ENG-20260601-05
压轴题二度验证: null  # 仅 tags 含 #压轴题：null=未一度验证 | "一度通过"=组卷做对1次待二度 | 二度通过后删除并 mastery→4 毕业
关联组卷: ["exam_YYMMDD_序号"]  # 可选，多值；稳定 stem（无 .md）；在 02_自定义组卷/未完成|已完成/ 解析；无则 []
variants: ["L1·换问法：…", "L2·加条件：…"]  # 数学必填结构化；见 §3.3
variant_history: []  # 已用过的变式策略摘要；组卷轮换时追加
variant_level_streak: {}  # 单变式策略连续做对次数；满2次下次升L档
知识专题: ""  # 可选；basename，解析见 05_不熟练知识专区/{科目}/{子类}/；见 §3.7
---

# [概括题目]

## 1. 原题 with 错题重现
![原题](../../../assets/images/[图片名].png)
(OCR 提取原题完整文本)

## 2. 错因分析
* 核心错因：**[代码] 描述**。通过分析萌萌的答题轨迹，指出其能力的具体卡点或防御心结（录入时「我看她的作答」迁入此处，可精炼）。
* **\[M1] 不会 · 强制附段**：当 `error_type` 为 **`[M1] 不会`** 时，在 §2 下必须增加 **`### 概念补洞（[M1] 必填）`**，用 3–6 条大白话写清本题依赖的**概念/定义/判法**（可含易混辨析）；萌萌若反馈「不知道某某是什么意思」，该词必须在此解释后再写解题步骤。
* **英语 · `[E1] 不会` 或 `mastery: 1` 必填**：§2 下增加 **`### 概念要点（必填）`**——中文词义或语法规则 + 1–2 条 ✓/✗ **英文例句**（供组卷 **[概念热身]** 提炼）；`[E2]` 但 mastery 1 时写语法规则要点。

## 3. 正确解析 (SOP)
* 解题题眼：**【用加粗双主括号标注核心突破钥匙】**。
* 正确过程：
    1. 最小可行性步骤（MVP）1。
    2. 最小可行性步骤（MVP）2。

## 4. 本质分析（必填，全科目统一）
* **定位**：本题「到底在考什么、怎么想」的提炼，供家长口头带练；与 §3 SOP（解题步骤）分工——§3 写给做题，§4 写给懂题。录入时「你在家可以这样带」迁入 **`### 展开分析`** 首条或单独一条。
* **结构**：
  * **`### 一句话快速概括`**：引用块 `>` 写 1 句，可直接念给孩子。
  * **`### 展开分析`**：2–4 条，写清概念辨析、主流程、易漏点；英语可标 **语法** / **词汇**。
* 能一句话说清时仍须同时写两小节（概括 + 至少 2 条展开）。
* 示例格式：
    1. **语法**：……
    2. **词汇**：……
```

### 3.4 组卷与批改反馈流 (Exam & Feedback Loop)

#### 5.0 每次组卷/出 PDF 前必读（强制加载）

| 类型 | 路径 |
|:---|:---|
| 总纲 | 本文件 §3.4 |
| 错题中台规则 | `萌萌错题记录/.cursorrules` §5 |
| 操作手册 | `萌萌错题记录/02_自定义组卷/组卷打印说明.md` |
| HTML 排版模板 | `萌萌错题记录/02_自定义组卷/_exam_template.html` |
| SVG 片段库 | `萌萌错题记录/02_自定义组卷/_snippets/` |
| PDF 脚本 | `萌萌错题记录/scripts/md_to_exam_pdf.py` |
| 题库 | `萌萌错题记录/01_错题库/...` |
| 默写词表 | `萌萌错题记录/04_语文默写清单/默写词表.md` |
| 图片资产 | `萌萌错题记录/assets/images/` |
| 默写卷规则 | 本文件 **§3.5**、`04_语文默写清单/README.md` |

**产出两件套**：`exam_[YYMMDD]_[序号]` 或 `dictation_[YYMMDD]_[序号]` 的 `.md` + `.pdf`（由 `md_to_exam_pdf.py` 从 md 生成 pdf；新卷落盘 `未完成/`；批改完成后 md+pdf **移至** `已完成/`）

#### 5.1 智能检索逻辑（含 Sprint Review Slot）
*   **默认组卷规模**：**10 题**，实行 **9 + 1** 混合抽取（Sprint Review Slot / 冲刺回顾槽）。
*   **9 道 · 在练槽**：扫描各模块**在练目录**（排除 `已毕业/`），过滤 `mastery < 4`（历史 `mastery: 5` 若在练视同 `<4` 待迁移）；按 **`错误次数` 加权抽取**（权重 = `max(1, 错误次数)`，次数越高越易入选；无放回）；母亲指定「随机」时可均等随机。实现参考 `萌萌错题记录/scripts/pick_exam_weighted.py`。
*   **1 道 · 冲刺回顾槽（艾宾浩斯）**：从全库各模块 `已毕业/` **按间隔到期**抽取 **1** 道（`mastery: 4` 或历史 5）；实现参考 `萌萌错题记录/scripts/pick_review_spaced.py`；英语答题卡卷题面 **`N、〔复检〕`** 连写；`mapping` 对应项 `slot: review`（其余 `active`）。确认清单须附 **`next_review` / 逾期天数**。
*   **题量非 10**：母亲指定总数 N 时，回顾槽 = **`已毕业/` 非空则固定 1**，其余从在练槽补足；`已毕业/` 为空则回顾槽为 0，全从在练抽取并提示。
*   **关键字抽取**：筛选条件作用于**在练槽**（如“几何压轴题”→ `module` + `tags`）；回顾槽仍独立按 **[长线记忆复检 · 艾宾浩斯]** 抽 1 道（除非 `已毕业/` 为空）。
*   **超纲过滤（数学）**：默认在练槽**排除** `tags` 含 `#超纲`；母亲说「含超纲」则纳入，「只组超纲」则仅抽 `#超纲` 题。
*   **确认清单**：组卷前**必须先以列表向母亲确认**全部题目，标注「在练」/「复检」、**变式策略摘要**（数学，见 **[数学变式引擎]**）、**概念热身**（数学/英语，列概念名）、**组卷要求**（`mapping` 题挂 `知识专题` 时，摘自专题 YAML 摘要）；母亲回 **`组`** / **`按推荐组卷`** 即确认，**无需**逐题审变式或热身正文。

    **含变式的数学题强制附「变式质检表」（落盘前 AI 自检，母亲零额外工作）：**

    > 每道有变式（N.1）的题必须在清单里附下列一行（三项均需填写，任一项未通过则重出变式不得落盘）：
    >
    > | 题号 | 变式摘要（≤20字） | 与母题建模差异（一句话） | 脱离母题独立建模 |
    > |:---:|:---|:---|:---:|
    > | 1.1 | 含权绝对值，最小值落单点 | 母题最小值是区间段；变式斜率突变至单点，需重新判分段结构 | ✅ |
    > | 4.1 | 给占比反推数量+两类互比 | 母题「给数量求占比」；变式「给占比求数量，再两类互比」，建模方向相反 | ✅ |
    >
    > **✅ 判据**：萌萌完全不看母题，能从头建立逻辑链、得出答案。  
    > **❌ 判据**：套母题步骤换个数即可完成 → 必须重出，不得写入 md。  
    > 整卷有任意一题变式 ❌ → 所有含变式题一并重跑三步自检，不只改那一题。
*   **知识专题（组卷读取）**：`mapping` 母题 md 若有 **`知识专题`**，组卷时**打开** `05_不熟练知识专区/…/{basename}.md`，读 **`组卷要求`** + `## 规则精讲`，设计变式/干扰项/概念热身；与 `variants` 冲突时**以专题 `组卷要求` 为准**（仍须 **§5.2.2 可争辩：无**）。同卷多题同一专题 → 要求只读一次。详见 **§3.7**。
*   **废止**：错题 YAML **`组卷备注`** 已废止；历史档挂链专题时删除。专项组卷指令只写专题 **`组卷要求`**。

#### [长线记忆复检 · 艾宾浩斯]（`slot: review` · 已毕业题）

> **目的**：毕业题不再随机刷量，按遗忘曲线**到期才抽、逾期越久越优先**。

**YAML 字段**（仅 `已毕业/` 档维护；移回在练时删除）

| 字段 | 含义 |
|:---|:---|
| `review_level` | 间隔档位 `0–5`（默认 `0`） |
| `next_review` | 下次应复习日期 `YYYY-MM-DD` |

**间隔序列（天）**：`1 → 3 → 7 → 14 → 30 → 60`（`review_level` 对应下一档间隔）

| 时机 | 动作 |
|:---|:---|
| **毕业**（`mastery→4` 移入 `已毕业/`） | `review_level: 0`；`next_review` = 毕业日 **+1 天** |
| **复检主题做对** | `review_level +1`（封顶 5）；`next_review` = 批改日 + **下一档**天数 |
| **复检主题做错** | 现有规则（`错误次数+1`、`mastery→1` 移回在练）；**删除** `review_level` / `next_review` |

**组卷抽 1 道**（`pick_review_spaced.py`）

1. **到期池**：`next_review ≤ 今天`；无 `next_review` 时用 `关联组卷` 最近 `完成日期` + 当前 `review_level` 推算；仍无记录 → **视为到期**。
2. **加权**：到期池内按**逾期天数**加权（越久权重越高）。
3. **兜底**：到期池为空 → 取 `next_review` **最早**的 1 道，确认清单注明「无逾期·最近到期」。

**复检卷面**：默认**仅主题、无变式**（见 **[数学变式引擎]** `mastery 4 / 复检槽`）。

#### 5.2 组卷规范 (Exam Generation)
*   **物理路径（Finder 态 · basename 终生不变）**：
    *   新卷：`02_自定义组卷/未完成/exam_[YYMMDD]_[序号].{md,pdf}`
    *   批改完成：`02_自定义组卷/已完成/exam_[YYMMDD]_[序号].{md,pdf}`（**移动** md+pdf；**禁止**改 basename、**禁止**在文件名加 `【】`/Emoji 前缀；**禁止**生成或保留同 stem 的 `.html`）
    *   **稳定 stem**：`exam_[YYMMDD]_[序号]` — 用于 `exam_id`、错题 `关联组卷`、卷内 `(ID: …)`、批改口令；**禁止**把 `【…】` 或子目录名写入 `关联组卷`
    *   **stem 解析**：按 stem 在 `未完成/`、`已完成/` **各查一次**（`{stem}.md`），命中唯一路径后读写；禁止假定文件在组卷根目录
    *   **当日序号**：分配新 `exam_[YYMMDD]_` 序号时，在 `未完成/` + `已完成/` **合并扫描**取 max+1
*   **展示标题（非物理名）**：YAML `title`、卷面 H1 **不写**完成状态前缀（禁止 `【⬜未完成】` / `【✅已完成】` 及顶部状态徽章）；用 `萌萌 [科目] 复习卷 (ID: …)` 或带专项说明的标题即可。PDF 书签读 YAML `title`。
*   **试卷结构**：YAML 含 `mapping`（外显题号 → 错题 `id`；每项含 `slot: active | review`）、`answers`（**数学/语文 卷必填**；英语见 §5.2.1）、`answer_sheet_mode`（**`math`** 数学新卷默认 · **`english`** · legacy **`math_selective`**）、`完成日期`（`[]` 表示未完成；多次做完可记多个 `"YYYY-MM-DD"`）。
*   **完成日期**：整卷批改结束后写入当卷 YAML；未做完保持 `[]`；**同步**：① md+pdf 从 `未完成/` **移至** `已完成/`；② 按需重跑 `md_to_exam_pdf.py` 刷新 pdf。
*   **排版规则**：
    *   **禁止完成状态徽章**：卷面 H1 **上方不得**出现 `exam-status` 状态行；H1 **不带**【⬜未完成】/【✅已完成】前缀。
    *   **标题**：`# 萌萌 [科目] 复习卷 (ID: YYMMDD序号)`（或 `# 萌萌数学·K4数论专项…`）
    *   **概念热身（数学 / 英语）**：若本卷含 **[概念热身]** 触发题，H1 下、第一题（或「第一部分」）前插入 `## 概念热身`（见该节）；**不算题、不占题号**。英语答题卡卷 **例外**：仅此节引导语可用 blockquote（仍禁止 H1 下批改说明类 blockquote）。
    *   **全科目统一**：卷面**禁止**「建议用时」；**数学/语文旧式卷**卷头可写批改示例。**英语 `answer_sheet_mode: english`**：卷面**不写**批改说明 blockquote（见 §5.2.1）。
    *   **英语组卷排版（`subject: 英语` 时另加 · 须 `answer_sheet_mode: english`）**：
        *   **第一部分题卷**：**`N、`+题干同行**；语法/单选等可有 **`N.1、`** 变式；**E3 阅读填空无变式**（见 **[数学变式引擎]** 英语阅读例外）。**禁止**考点名、ENG-ID、**【变式挑战】** 标题；**禁止**在题卷作答。
        *   有变式的题：答案写在答题卡「变式」列；**阅读填空**变式列 **—**。
        *   英语阅读/完形超长：长文在题卷，客观题选项在答题卡（见 §5.2.1 E3 预留）。
    *   **数学组卷排版（`answer_sheet_mode: math` · 新卷默认）**：
        *   每题（含变式 `N.1、`）包在 `<div class="q-item">` 内：**加粗题号** + 题干 + 题下 `<div class="q-calc">` 虚线答题区（无底色、无「作答」）。
        *   `q-calc` 高度：`short` 填空/选择/判断；`mid` 计算/多空；`tall` 应用题。
        *   **无** `## 第二部分 · 答题卡`；标准答案用 `## 第二部分 · 标准答案`（或 `## 标准答案`）。
        *   变式 **`N.1、`** 与主题同样式；**禁止**【变式挑战】标题。
    *   **数学 / 语文 legacy 组卷**（无 `answer_sheet_mode` 或 `math_selective`）：可加答题留白区；**不写**建议用时。语文**字形默写**不走 `exam_*`，见 **§3.5 `dictation_*`**。
    *   **多媒体展示准则**（按「是否依赖图形审题」区分，勿因属 K2 模块就一律贴图）：
        *   **含几何图形题**（组合图形、割补示意图、立体图、展开图等，**无图无法审题**）：**必须保留**原题截图。组卷卷子引用：`![原题](../../assets/images/[图片名])`（`未完成/`、`已完成/` 下 md 用 `../../`；错题库 md 仍用 `../../../assets/images/`）。
        *   **纯文字题**（含 K2 表面积/体积文字应用、选择题无配图、教辅页整页拍照仅作存档）：**禁止**在试卷中贴 workbook 截图，须 OCR 整洁排版并留答题区。
        *   **K1/K3/K4 模块**：同上，一律纯文字排版，禁止贴图。
        *   **变式需新几何图**：在 `exam_*.md` 正文**内嵌 SVG**（可复制 `02_自定义组卷/_snippets/` 片段为 raw HTML），禁止贴教辅截图。
        *   **长文本阅读保底（现代文 / 完形填空等）**：纯文本原题 OCR 后**超过 300 字**，须先尝试整洁排版；排版后**自我审查**：若换行错乱 **>3 处** 或标点全/半角严重混杂，则启用保底——改用原题**高清截图**排版，并附说明：「为保真，此处已切换为原题截图。」
    *   **错题中台子模块**：组卷强制条款与 [`萌萌错题记录/.cursorrules`](萌萌错题记录/.cursorrules) §5 保持同步；排版模板 `02_自定义组卷/_exam_template.html`，操作手册 `02_自定义组卷/组卷打印说明.md`（手册不替代规则）。
    *   **变式**：见 **[数学变式引擎]**（数学）及下条通用规则；**英语答题卡卷**用 **`N.1、`** 印变式题干；**数学/语文 legacy 卷**用【变式挑战】+ 答题留白；**须**卷末 **`## 标准答案`**（与 YAML `answers` 同步）。变式判错**自动新建**错题档（§3.3）。
*   **卷面结构（见 §5.2.1）**：
    *   **`math`**：第一部分题目（题下虚线答题区）+ 第二部分标准答案 + 附映射；**一个 PDF**。
    *   **`english` / `math_selective`**：题·卡·答案**三节**；合并为一个 PDF。
    *   **数学/语文 legacy**（无 `answer_sheet_mode`）：卷末 **`## 标准答案`** + YAML `answers`；`dictation_*` 不适用。
*   **PDF 交付（必做 · 死命令）**：组卷落盘 `.md` 后，**你必须在终端主动执行**（工作目录 `萌萌错题记录/`）：
    `python3 scripts/md_to_exam_pdf.py "02_自定义组卷/未完成/exam_….md"`（或 `已完成/` 下路径；见 [`组卷打印说明.md`](萌萌错题记录/02_自定义组卷/组卷打印说明.md)），生成**同目录**同名 `.pdf`（脚本内部用临时 HTML，**不**写入卷子目录）。PDF 须 A4、保留答题留白与几何图；成功后向母亲给出 **PDF 完整路径**。**仅当**脚本报错且 **debug 重试 1 次仍失败** 时，方可将命令与错误信息交给母亲。**禁止**只交 `.md` 不尝试 PDF；**禁止**在 `未完成/`、`已完成/` 下创建 `exam_*.html`。

#### 5.2.2 英语组卷·选项唯一性（强制 · 2026-06 起）

> 与 [`萌萌错题记录/.cursorrules`](萌萌错题记录/.cursorrules) **§5.2.2** 同步。组卷**禁止可争辩题**：除标答外，干扰项不得被家长/语法书合理判对；未通过则改题或换题。**`fish`/`fishes`**：禁止无 *kinds/species* 提示却唯一收 *fishes* 的池塘题；禁止复刻 `exam_260603_02` 第 4.1 双空模式。确认清单每题须 **`可争辩：无`** 方可落盘。

#### 5.2.1 卷面结构（数学 inline · 英语/legacy 答题卡）

> **批改**：标准答案在 YAML `answers`，AI **从工作区读取**，无需把答案页传入对话。  
> **旧卷**：YAML **无** `answer_sheet_mode` 的 `exam_*` **禁止 retrofit**。

**数学 · `answer_sheet_mode: math`（新卷默认）**

| 项 | 约定 |
|:---|:---|
| 结构 | `## 第一部分 · 题目` → `## 第二部分 · 标准答案` → `## 附：题库映射`（可选 `## 深度分析`） |
| 每题 | `<div class="q-item">` + 题干 + `<div class="q-calc short\|mid\|tall">` |
| 题号 | **`N、`** / **`N.1、`** 加粗（`md_to_exam_pdf.py` 自动） |
| 作答 | 题下虚线框；**无**独立答题卡 |
| 复检 | `N、〔复检〕` + 题干连写 |
| 批改 | `批改 exam_stem：1对，1.1错`；可拍题下答题区 |

**YAML 片段（数学）**

```yaml
answer_sheet_mode: math
answers:
  1: { main: "26", "1.1": "25" }
mapping:
  1: { id: MATH-20260614-01, slot: active, qtype: 填空题 }
  7: { id: MATH-20260514-06, slot: review, qtype: 应用题 }
```

**英语 / `math_selective` · 答题卡三件套**

> 批改时母亲**优先上传答题卡**（或文字对错），不上传整卷题干，省 token。

| 项 | 约定 |
|:---|:---|
| **适用范围** | **`english`**：英语全科。**`math_selective`**：legacy，选择/单空上卡，应用在题卷留白。语文 `exam_*` 暂不启用。**`dictation_*`** 排除。 |
| **物理交付** | **一个 PDF、三节**（打印时只给孩子前两节；第三节勿发给孩子，或你自行撕页）。 |
| **题号** | 主题 **`1`**，变式 **`1.1`**（与卷面 **`1.1、`** 对应）。 |
| **英语作答** | **所有最终答案只写在第二部分答题卡**；第一部分**只印题干+选项**，题卷括号内**禁止**作答。句型转换/长句：题卷可草稿，**誊写最终句到答题卡**；争议时母亲可补该题**局部**照片。 |
| **题卷编号** | **`N、`与题干同一行**（如 `1、Electrical appliances…`）；变式 **`N.1、`与题干同一行**；`slot: review` 写作 **`9、〔复检〕`** + 题干连写。**禁止**单独一行只写序号、禁止考点名/ENG-ID。 |
| **卷面说明** | H1 下**禁止** blockquote（批改示例、题量说明等）；**例外**：`## 概念热身` 内引导语可用 blockquote；**禁止**顶部完成状态徽章；英语给孩子部分只保留 H1、三节标题与题干。 |
| **答题卡留白** | **新卷默认** `table.sheet` + **`blank-short`**（单选/判断，**不印** ABCD）；填空/阅读用 **`blank-line`**。Legacy 才可用 `sheet-mc`/`sheet-opt`。 |
| **数学混合卷** | `sheet: true` 的题：答题卡填选项/数字；`sheet: false` 的题：不在答题卡出现，仍在题卷答题区。 |
| **长线记忆复检** | 题号**连续**。`slot: review`：题卷 **`9、〔复检〕`** + 题干连写；答题卡同行 **`〔复检〕`**；确认清单/映射表标 review。批改逻辑同在练（错则 `错误次数`+1、`mastery`→1）。 |
| **E3 阅读/完形（预留）** | 长文在**第一部分**；答题卡写「阅读见题卷第 N 页」，只印 **21–25** 等客观题填涂区。 |

**YAML（新卷必填片段）**

```yaml
answer_sheet_mode: english   # english | math_selective
answers:
  1: { main: "Finally", variant: "Actually" }
  2: { main: "D", variant: "A" }
mapping:
  1: { id: ENG-20260601-01, slot: active }
  9: { id: MATH-20260520-03, slot: review }   # 答题卡第 9 行带〔复检〕
```

* **`answers`**：组卷时由 AI 填写；`main` = 主题，`variant` = 变式 `n.1`。**选择**存字母；**填空**默认**大小写不敏感**比对，YAML 可加 `main_case_sensitive: true` 当专有名词等必须大写。**句型/长句**存标准句字符串。
* **第三部分**：与 `answers` 同步生成可读对照表（家长核对）；与 md 同源，PDF 第三节一并输出。

**组卷 md 结构（三节标题固定）**

1. `## 第一部分 · 题目` — H1（**无**卷头 blockquote、**无**状态徽章）；各题 **`N、`+题干同行**（**无**考点名/ID）；可按题型 **`### **一、单选题**`** 等分段；英语题卷无作答区。
2. `## 第二部分 · 答题卡` — 表 `class="sheet"` + `sheet-answer`；**单选题/判断题**用 **`blank-short`**（**不印** A/B/C/D）；填空/阅读用 **`blank-line`**；`slot: review` 行题号后 `〔复检〕`。
3. `## 第三部分 · 标准答案` — 题号 + 标准答案（含全部变式）。
4. **`## 深度分析`**（可选节名固定）— **`错误次数≥2` 的 mapping 题必写**；见 **[深度分析]**；**不发孩子**。
5. `## 附：题库映射` — 批改用对照表。

模板见 `02_自定义组卷/_exam_template.html` · 操作见 `组卷打印说明.md`。

#### [数学变式引擎]（v3.1 · 组卷默认开启 · 母亲零配置）

> **生效范围**：**新组卷**（落盘 `未完成/` 的 `exam_*`）。**`已完成/` 旧卷禁止 retrofit**。未进过已完成卷的在练题，`variants` 仍可按 **[数学 variants 模块默认表]** fallback，**禁止**批量改历史题库（除非母亲明示「批量升级 variants」）。

**变式层级（禁止 L0 纯换常数）**

| 层级 | 动作 |
|:---:|:---|
| L1 | 换问法（求和→求组数/最大最小） |
| L2 | 加/改条件（范围、奇偶、互素附加约束） |
| L3 | 边界/反例（初始状态、陷阱词） |
| L4 | 换场景同模（K3 优先） |

**`variants` 写法强制规范（录入时必须遵守 · 组卷 fallback 同）**

> 目标：让任何一个未来的 AI 读到 `variants` 都能直接设计出「不看母题独立建模」的合格变式。

- **禁止**出现「换系数」「换数字」「换场景」「换动物/人物」「换单位」等描述——这些描述会诱导 AI 输出只换表面的低级套壳变式。
- **必须**写清「改的是什么建模入口」，格式：`"L1·[改什么]：[一句话说做了什么操作改变]"`。
  - ✓ `"L1·改问法方向：给出各类占比反推数量，再求两类之间的比（非「给数量求占比」）"`
  - ✓ `"L2·加约束：b 为正整数，求含权绝对值式最小值及对应 b 值"`
  - ✓ `"L4·换第二问目标量：改问「距离下一个标记还差几米」而非「另一方走了多远」"`
  - ✗ `"L1·换场景同模：三摊主蔬菜，价格数量全换，专考销售额占比"`（场景换了但建模入口未变）
  - ✗ `"L1·换全套系数和关键点，重跑分段法"`（仍是同公式换数）
- **必须能通过正向测试**：读完 `variants` 描述后，如果萌萌不看母题就能按描述独立建模、得出答案——合格。若她只需「套母题步骤换个数」——不合格，重写。

**整卷题量预算制（v3.2 · 以深度换广度）**

> 核心原则：目标是搞懂每道题，不是刷题量。变式需求越高时主动压缩母题数量。**总格数上限 ≤ 15**（母题编号 + 变式编号 N.1 之总和）。

```
复检槽：固定 1 格（slot: review，不配变式）
有效预算：14 格

① 识别"需变式"的在练题：
   - 错误次数 ≥ 2         → 必须配变式（高优先级）
   - mastery = 1 且错误次数 = 1 → 应配变式（低优先级）
   设识别出 N_identified 道

② 边界截断（防负数）：
   N_effective = min(N_identified, 7)    ← 上界 = floor(14/2)
   若 N_identified > 7，按优先级保留前 7 道含变式，其余降级为无变式母题

③ 含变式母题：N_effective 道，占 2×N_effective 格
   无变式母题：14 - 2×N_effective 格（恒 ≥ 0）
   总格数 = 2×N_effective + (14 - 2×N_effective) + 1 = 15  ✓
```

| N_identified | N_effective | 含变式母题 | 无变式母题 | 复检 | 总格数 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 0 | 0 | 0 | 14 | 1 | 15 |
| 3 | 3 | 3 | 8 | 1 | 15 |
| 7 | 7 | 7 | 0 | 1 | 15 |
| ≥8 | **7（截断）** | 7 | 0 | 1 | 15 |

截断时确认清单注明：「共 X 道需变式题，本卷截断至 7 道；其余 X-7 道降为无变式母题或移入下一卷。」

**降级顺序**（N_identified > 7 时从低优先级开始截断）：
1. mastery=1 且 `错误次数 = 1` 的题先降
2. `错误次数 ≥ 2` 中 mastery 最低的题次降
3. 超出部分加入下一卷候选池，不丢弃

**变式 L 档分配**（读母题 YAML `mastery`；历史 5 若在练按 3 处理）

| mastery | 变式数 | 策略 |
|:---:|:---:|:---|
| **1–2** 很不懂/略懂 | **1** | 从 `variants` 取 **L1**（换问法优先）；legacy 用【变式挑战 N.1】 |
| **3** 懂但粗心 | **1** | 从 `variants` **轮换**（跳过 `variant_history`）；单变式 **连续做对 2 次** → 下次 **升 L 档**（L1→L2→L3） |
| **4** / 复检槽 | **0** | 复检默认**仅主题**，不配变式 |

> ⚠️ **废止**：「mastery 1-2 → 2 道变式（N.1 + N.2）」。新规 mastery 1-2 统一最多 **1 道**。

**模型迁移验证（落盘前强制三步自检 · 与 §5.0.1 五条硬约束并列）**

变式设计完成后，落盘前必须通过以下三步，任一未通过则重出：

**Step 1 · 草稿纸排查**
把母题 §3 正确解析（SOP）完整走一遍，列出所有中间值。变式的**最终答案**不得与任何中间值相同或为其直接推论。
命中即重出：「变式答案 = 母题某一步的计算结果」。

**Step 2 · 数值基底重置**
变式中所有关键初始数据（参与建立方程/模型的数字）必须与母题**全部不同**。禁止题干出现「仍为上题」/「仍用 X 题数据」，每道变式必须自足。
命中即重出：母题任何初始数字原封不动出现在变式中。

**Step 3 · 问法维度检验**
禁令：
- 禁止「前置步骤」变式：变式不能是解母题时必须经过的中间环节
- 禁止「顺手附加」变式：不能在母题答案基础上再算一步就得出变式答案
- 禁止「低级套壳」变式：不能只替换数字、名词、单位、动物/人物、加减号或目标分数，题干句式和作答动作仍与母题一致
- **L1 最低门槛**：至少改变一个真实作答入口——条件表达方式、目标量、问法方向或验算任务；若孩子做完母题后能不重新建模、按同一行公式替换数值完成，则判为不合格，必须重出
- **典型禁例**：
  - 分数条件题：母题是「分子/分母加减 → 某分数 → 求原分数」，变式不能仍是同一句式只换加减数和目标分数
  - 周期/最小公倍数题：母题问「谁先到标记，另一方走了多远」，变式不能只换动物、步长和间距后仍问同一件事；至少改问「离下一个标记还差多少 / 还需几次 / 第几个标记」之一

**合格判据（正向）**：萌萌把变式题单独拿出来，**完全不看母题**，也能从头建立逻辑链、得出答案。

**英语 · 阅读/概要（强制覆盖上表）**

| 条件 | 变式 |
|:---|:---|
| `mapping[n].qtype` 为 **阅读填空** / **完形填空**，或母题模块 **E3.阅读与完形** 且题为「读短文 + 概要/填空」 | **0** · **禁止** `N.1`/`N.2`；答题卡「变式」列填 **—**；`answers` 仅 `main`（多空用分号或 `(1)…(5)…`） |
| E2 单选题等 | 仍按上表 mastery 给变式 |

**英语 · 阅读+概要卷面（录入与组卷一致）**

* **§1 / 题卷**：**完整阅读短文** + **完整概要段**（五空等须全文可见）；**禁止**只列「摘要提纲」或 `…` 省略句。
* **空位**：用 `______`（≥4 下划线）或 `<span class="q-blank"></span>`；`md_to_exam_pdf.py` 会将 `______` 转为可见横线。
* 概要空可标 **(1)(2)…** 与答题卡、标准答案对照。

**组卷执行顺序（数学 / 英语）**：扫不会题 → **[概念热身]**（若有）→（数学）§4 + `variants` + 变式 → 第一部分（**按 qtype 分子标题**；数学每题 **`q-item`+`q-calc`**）→（英语/`math_selective` **答题卡**）→ `answers` + 标准答案表 → **[深度分析]**（`错误次数≥2` 题 **必写**）→ 附映射 → 确认清单（附变式 + 概念热身 + **深度分析题号**）。

**YAML `answers`（数学 · 单变式）**

```yaml
answers:
  1: { main: "25 或 11", "1.1": "13" }   # 变式数值基底已重置，独立可解
  5: { main: "600", "5.1": "25" }         # mastery 3，轮换变式
```

* 每题仅键 `"N.1"`（废止 `"N.2"`）；legacy 卷 `## 标准答案` 表同步列出。

#### [概念热身]（数学 / 英语 · `mastery` 1 / 不会 · 卷顶自动插入）

> **目的**：本卷若有「概念/规则没建立」的题，**做题前先补理论**；组卷时**自动写入** H1 下，母亲**无需**另审（确认清单附一行「概念热身：互素 / furniture 不可数…」）。**`已完成/` 旧卷禁止 retrofit**。

**触发条件**（`mapping` 中 **`slot: active`** 任一道母题）

| 科目 | 触发 |
|:---|:---|
| **数学 / 英语** | **`mastery == 1`（很不懂）** — **唯一开关** |

* **不**因略懂/粗心（`mastery` 2–3）触发；**复检槽** `slot: review` 默认不写入。
* **`error_type` 含 `[M1]`/`[E1]` 不单独触发**（录入归档用；批改升档**不改** `error_type`，避免历史「不会」在已升档后仍误触热身）。
* 组卷再错打回 **`mastery → 1`** 时，概念热身**恢复**。
* **英语专名**：国名/人名/地名若仅为专名不熟，热身区可写 **1 行认读**（英文 + 中文）；**主数据**仍在 `03_英语默写/专有名词词表.md`，热身不替代词表复习。

**卷面位置**（H1 正下方、第一道题 / 「第一部分 · 题目」之前）

**数学 legacy 示例**

```markdown
# 萌萌 数学 复习卷 (ID: YYMMDDnn)

## 概念热身

> 本卷有几道题用到下面的词。**先看懂再做题**；做题时也可以翻回来查。

### 互素（也叫互质）

- **意思**：两个数除了 1 以外，没有共同的因数。
- **怎么判**：看除 1 以外有没有相同因数；或算**最大公因数**是否为 1。
- **例子**：3 和 8 → 互素。2 和 12 → 不互素（都有 2）。
- **别和「素数」搞混**：素数说的是**一个数**；互素说的是**两个数之间**的关系。

---

## 第 1 题 · …
```

**英语答题卡卷示例**（`answer_sheet_mode: english`）

```markdown
# 萌萌 英语 复习卷 (ID: YYMMDDnn)

## 概念热身

> 本卷有几道题考下面的用法。**先看懂再做题**；做题时也可以翻回来查。

### furniture（家具）— 不可数

- **意思**：furniture 指「家具」总称，英文里**不能加 s**。
- **怎么用**：much furniture / a lot of furniture / **plenty of** furniture（plenty 后面要有 **of**）。
- **别搞混**：many **chairs**（很多把椅子）✓；many **furnitures** ✗。
- **例子**：✓ We have much furniture.　✗ plenty furniture / many furnitures

## 第一部分 · 题目

1、We have ______ in our new flat.
…
```

**YAML（可选）**

```yaml
concept_primer: ["互素"]              # 数学
concept_primer: ["furniture 不可数"]  # 英语
```

**内容来源**：仅收集本卷 **`mastery == 1`** 的 mapping 题；母题 §2 + §4；数学优先 §2 **概念补洞**；英语优先 §2 **概念要点**（无则 §4）；**去重合并**，整卷 **≤2 个概念块**。无 `mastery == 1` 题则省略整节。

**录入侧（英语 · 便于组卷提炼）**：英语 **`[E1] 不会`** 或 **`mastery: 1`** 归档时，§2 须增 **`### 概念要点（必填）`**（中文词义/语法规则 + 1–2 条 ✓✗ 英文例句）；`[E2]` 但 mastery 1 时写**语法规则要点**（如不可数、冠词）。数学仍为 §2 **概念补洞（[M1] 必填）**。

**话术规范（读者：预备班 / 初一衔接 · 约 11 岁）**

| 要 | 不要 |
|:---|:---|
| 短句；**意思 → 怎么用/怎么判 → 例子** | 教材长定义、未解释符号 |
| **数学**：中文 + 小整数例子 | 幼儿腔（宝贝、超级简单） |
| **英语**：**中文讲规则** + **✓/✗ 英文例句**对照（关键词可保留英文） | 全篇中文无英文例、或整段英文无中文 |
| 易混用 **「别和…搞混」** 一行点破 | 讽刺、威胁式语气 |
| 每概念 **3–5 条** bullet | 把 §3 完整解题步骤抄进热身区 |

**生成顺序**：扫 mapping → 筛 **`mastery == 1`** 的 active 题 → 读 §2/§4 → 合并改写 → 写题干/变式/`answers`；无则省略 `## 概念热身` 与 YAML `concept_primer`。

**标准答案**：概念热身**不出题、不记分**；`answers` 与答题卡**不含**热身内容。

#### [深度分析]（新组卷 · 错题库 `错误次数` ≥ 2 · 家长区）

> **目的**：同一母题在组卷/批改中**反复错**（`错误次数 ≥ 2`）时，除卷首 **[概念热身]** 外，在答案区再写**可口头带练的展开**；与概念热身分工——热身=做题前速查，深度分析=对答案后讲透、含卷面变式对照。  
> **`已完成/` 旧卷禁止 retrofit**（除非母亲明示改某卷）。

**触发（组卷时扫 `mapping` 各题错题 md 的 YAML `错误次数`）**

| 条件 | 动作 |
|:---|:---|
| **`错误次数` ≥ 2** | **必须**写入本节，按卷面题号逐题一块 |
| `错误次数` = 1 | **不写**（除非母亲点名「某题也要深度分析」） |
| 概念热身 | 与深度分析**可重叠**（如 furniture、双重所有格）：热身短、此处展开+变式对照表 |

**卷面位置（固定顺序）**

1. `## 第一部分 · 题目` — 可按题型用 **`### **一、单选题**`** 等子标题（`###` 加粗，字号小于 `## 第一部分`）；`mapping[n].qtype` 用 **单选题** / **多选题** / **判断题** / **填空题** / **计算题** / **应用题** / **阅读填空** 等（**禁止**笼统写「选择题」）。  
2. `## 第二部分 · 答题卡`  
3. `## 第三部分 · 标准答案`（答案表 + YAML `answers` 同步）  
4. **`## 深度分析`** ← **本节**（在标准答案表之后、「附：题库映射」之前）  
5. `## 附：题库映射`

**打印**：第三节+第四节**勿发给孩子**（可撕页或只印前两节）；PDF 仍一节生成，家长自行处理。

**写法（给母亲 · 白话）**

- 文首一行 blockquote：说明**仅家长**、可撕页。  
- 每个触发题号：`### 第 N 题 · [考点口语]（[错题 id] · 错 N 次）`  
- 含：**结构/口诀 1 句**、**卷面主题+变式对照表**（正解 + 为什么不是干扰项）、**在家带一句**；数学可含步骤边界，英语含单复/搭配对照。  
- 内容来源：母题 §2 + §4（+ 数学 `[M1]` 概念补洞）；**禁止**抄整段 §3 解题过程。  
- 组卷确认清单附一行：`深度分析：第 1、2 题`（列出触发题号）。

**与排版改版同步**：改答题卡/题型子标题时**不得删**已写好的 `## 深度分析`；局部替换须保留本节。

#### 5.3 批改判定与逻辑熔断

> **原则**：组卷再错时**不修改**原错题 md 的 §2–§4 与 `error_type`；在同一 `id` 上 **`错误次数` +1** 且 **`mastery` 强制为 1**。变式判错 → **新建**独立错题 md（`#变式题` + `母题`）。

**批改输入（`answer_sheet_mode` 新卷 · 优先）**

| 方式 | 示例 | 说明 |
|:---|:---|:---|
| **文字** | `批改 exam_260602_03：1对，2错，1.1对` | **首选**（**数学 `math` / 英语均适用**）；AI 读 `answers` + `mapping`，无需传图 |
| **答题卡照片** | 上传答题卡截图 + `批改 exam_260602_03` | **英语** / legacy `math_selective` |
| **题下答题区照片** | 上传某题虚线框局部 + `批改 exam_260614_01` | **数学 `math`**；争议题可补拍 |
| **Override** | 同上 + `3改对` 或 `第3题按对处理` | AI 初判存疑或与你目视不一致时 |
| **整卷照片** | 旧卷或无 `answer_sheet_mode` | 兼容；新卷**避免** |

长句争议：母亲可对单题补**题卷局部图**；默认仍以答题卡誊写为准。  
**禁止**为批改把第三部分标准答案传入对话（答案已在 `exam_*.md`）。

当母亲反馈批改（如 `1对，2错` / `1.1错`）或 AI 自答题卡 OCR 判分后，执行：

1.  **主题做对**（`mapping` 指向已有错题 `id`）：
    *   **`#压轴题`**：仍走二度验证轨（`压轴题二度验证` / 毕业移入 `已毕业/`，同下）。
    *   **非压轴 · 升档**（**仅**当本卷分配变式**全部判对**时 +1 档；否则见下「维持」）：
        *   `mastery` 1→2、2→3、3→**4 毕业**（移入 `已毕业/`）；毕业时写入 **`review_level: 0`**、**`next_review`** = 批改日 +1 天（见 **[长线记忆复检 · 艾宾浩斯]**）。
    *   **`slot: review` 主题做对**（已毕业、无变式或变式全对）：**不升** `mastery`；按艾宾浩斯推进 `review_level` / `next_review`。
    *   **非压轴 · 维持**（**不升档**）：
        *   `mastery` **2**：主题对但 **2 道变式仅对 1 道** → **维持 2**。
        *   `mastery` **3**：主题对、**变式错** → **维持 3**（变式错另走第 3 条新建档；**主题档不跌**）。
    *   **`variant_level_streak`**：单变式模式下，该策略判对 → streak+1；**≥2** 则下次组卷升 L 档并 streak 清零；判错清零。
    *   **`variant_history`**：本卷用过的变式策略摘要 **追加**至母题 YAML（去重）。
    *   **不改动** `错误次数`（主题做对时；历次错误次数保留作组卷权重）。
2.  **主题做错**（`mapping` 指向已有错题 `id`）：
    *   **`错误次数`**：在原档案 YAML **+1**（仅改 frontmatter，**禁止**改 §2–§4）。
    *   **`mastery`**：**强制为 1**（无论原为 2–4 或历史 5；若原在 `已毕业/` 须移回在练并改图片路径）。
    *   **`#压轴题`**：先将 `压轴题二度验证` **置空**，再执行上两条。
    *   **`[长线记忆复检]`**（`slot: review`）：同上（`错误次数` +1、`mastery` → 1、移回在练）；**删除** `review_level` / `next_review`。
3.  **变式做错**（卷面 `N.1` / `N.2` /【变式挑战 X.Y】，无独立 `id`）：
    *   **自动新建**错题 md：`tags` 含 **`#变式题`**；`母题` = 主题 `id`；`错误次数: 1`；`source` = `{exam_stem}·变式X.Y`；题干 OCR 写入 §1。
    *   **`知识专题`**：与母题同值（母题有则继承）；执行 §3.7 **7.3** 关联维护。
    *   **不**修改母题正文；母题 `错误次数` **仅**在主题本身判错时 +1。
4.  **物理回写**：
    *   `mastery` → **4**（或历史写入 4）：移入 `已毕业/`，图片路径 `../../../../assets/images/`。
    *   更新 `关联组卷`、完成日期；卷子 md+pdf **移至** `已完成/`（§5.2）。
5.  **报告**：“✅ 批改完成：错误次数 +n，新建变式档 m，毕业 k。”

6.  **全对鼓励回复（`exam_*` / `memo_*` · 全题全对时强制附上）**：当本次批改结果为**所有主题与变式均判对**时，在批改报告之后**必须**附上一段给萌萌的鼓励话，供母亲直接给萌萌看。
    *   **语气**：温和、真诚，像一个认识她的家庭教师在夸她，不居高临下，不煽情，不堆空洞词（不说「你真棒」「你太厉害了」）。
    *   **内容**：具体指出这次做得好的地方——例如老题重做全对（说明真学进去了）、罕见模块或复检题通过、有主动质疑/验证的动作等；内容随当次卷面实际情况写，不套模板。
    *   **篇幅**：3–5 句，自然流畅，不列条目。
    *   **格式**：批改报告后加 `---`，标题 `**给萌萌：**`，正文另起段。

**废止（默认不再使用）**：主题再错时的 A/B/C/D 四档定性矩阵；除非母亲明确要求旧流程。

#### 5.3.1 压轴题二度验证（`#压轴题` · 毕业仍至 mastery 4）

* **一度前**：最高 **3**；组卷首次主题判对 → **3** + `压轴题二度验证: 一度通过`。
* **二度通过**：连续两次组卷主题判对（中间无该题判错）→ **4** 毕业，删除 `压轴题二度验证` 字段。
* **链断裂**：任一次主题判错 → 清空 `压轴题二度验证`，`mastery`→**1**。

### 3.5.1 词语默写艾宾浩斯（`memo_*` · 五类词表统一）

> 与 `01_错题库` **完全分离**。覆盖 **语文字形** + 英语 **专名/短语/句子/词性转换** 共 5 类词表；**新默写一律走 `memo_*`**（`dictation_*` 见 §3.5 legacy）。

#### 词表字段（五表统一追加）

| 列 | 含义 |
|:---|:---|
| `review_level` | `0–5`；写对 +1（封顶 5） |
| `next_review` | 下次应复习日 `YYYY-MM-DD` |

**间隔序列（天）**：`1 → 3 → 7 → 14 → 30 → 60`（与 **[长线记忆复检 · 艾宾浩斯]** 相同）

| 时机 | 词表操作 |
|:---|:---|
| **新词录入** | `状态=在练`，`复习次数=0`，`review_level=0`，`next_review=入库日+1` |
| **写对** | `复习次数+1`；`review_level+1`；`next_review=批改日+下一档`；`review_level≥2` → `状态=已掌握` |
| **写错/又忘** | `复习次数+1`；`状态=在练`，`review_level=0`，`next_review=批改日+1` |

**已掌握仍会进卷**：`next_review ≤ 今天` 时参与抽取（废止「连续对满 2 永不出卷」；`连续对` 列只读历史，新批改不再写）。

**老行无 `next_review`**：`在练` → 视为到期；`已掌握` → `review_level=2` 并按 `入库日期+14天` 推算。

#### 组卷（默认 2 类 × 8 题 = 16 题）

*   **物理路径**：`02_自定义组卷/未完成/memo_[YYMMDD]_[序号].{md,pdf}` → 完成后移至 `已完成/`；序号在 `未完成/`+`已完成/` 合并扫描 `memo_[YYMMDD]_` max+1。
*   **默认抽题**：自动选 **到期/在练压力最大** 的 **2** 类；每类按艾宾浩斯加权抽 **8** 条。实现：`scripts/pick_memo_spaced.py`、`scripts/build_memo_exam.py`。
*   **指定类型**：`组默写 短语+词性转换`（`+` 分隔，别名见 `03_英语默写/README.md`）。
*   **确认清单**：每类列条目 + `next_review` / 逾期天数 / 抽取来源（到期池/补足）；不足 8 条须注明「该类仅 N 条」。
*   **卷面**：每类一节；语文字形印拼音；英语按各类卷面规则（短语/句子给中文，词性转换给原词+词性+中文，专名给中文写英文）；卷末 `## 标准答案`（**三列**：题号 | 标准答案 | **已复习次数** = 组卷时词表 **`复习次数`**（历次已完成默写卷批改累计，不论对错）；YAML `mapping[n].review_count` 同步快照；**仅答案页、不印给孩子卷面**；本卷批改完成后每题 +1）。
*   **PDF**：`python3 scripts/md_to_exam_pdf.py "02_自定义组卷/未完成/memo_….md"`。

#### 批改

*   `批改 memo_260610_01：1对2错…` → `scripts/grade_memo.py` 逻辑回写词表；更新 `完成日期`；md+pdf 移至 `已完成/`。
*   报告：「✅ 默写批改完成，已更新词表 n 行。」

### 3.5 语文默写清单与默写卷 (`dictation_*` · legacy)

> 与 `01_错题库` **完全分离**。**新默写请用 §3.5.1 `memo_*`**；本节 `dictation_*` 仅兼容旧卷，**禁止 retrofit**。权威说明见 [`萌萌错题记录/04_语文默写清单/README.md`](萌萌错题记录/04_语文默写清单/README.md)。

#### 5.0 默写清单录入

*   **数据源**：`04_语文默写清单/默写词表.md`（词语 | 拼音 | 状态 | **复习次数** | **入库日期** | **年级** | 备注 | **review_level** | **next_review**；`连续对` 只读历史）。
*   **触发**：「伶俐默写错了」「把 XX 加进默写清单」等 → **只改词表一行**，无 OCR 反问、无 `chn_*.md`、无 Source/标签/错因。
*   **新词/再错**：按 **§3.5.1** 写 `review_level` / `next_review` / `状态`；拼音带声调；**入库日期**默认当天；**年级**按 **[年级自动换算]** 填写。

#### 5.1 默写卷生成

*   **物理路径**：与 `exam_*` 相同——新卷 `02_自定义组卷/未完成/dictation_[YYMMDD]_[序号].{md,pdf}`；完成后 md+pdf 移至 `已完成/`；basename 不变；**禁止** `dictation_*.html`。**稳定 stem** `dictation_[YYMMDD]_[序号]`（**禁止**写入错题 `关联组卷`）。
*   **序号**：在 `未完成/` + `已完成/` 内按**当日** `dictation_[YYMMDD]_` **合并**取最大序号 +1（与 `exam_*` 独立计数）。
*   **抽题**：默认词表全部 **`在练`**；母亲可指定词列表或题量 N。
*   **确认**：出卷前列出「词语（仅给你看）| 卷面拼音」清单，确认后落盘。
*   **卷面**：每题 `根据拼音写出汉字：{拼音}` + 横线留白；**禁止**印汉字、无原题图、**无【变式挑战】**、无建议用时。
*   **YAML 示例**：
    ```yaml
    exam_type: dictation
    dictation_id: dictation_260602_01
    subject: 语文
    title: "【⬜未完成】萌萌 语文默写卷"
    完成日期: []
    mapping:
      1: { word: 伶俐, pinyin: "líng lì" }
    ```
    （`word` 仅供映射与批改，**不出现在卷面**。）
*   **PDF**：落盘后执行 `python3 scripts/md_to_exam_pdf.py "02_自定义组卷/未完成/dictation_….md"`（与 `exam_*` 相同脚本；PDF 与 md 同目录）。

#### 5.2 默写卷批改（只回写词表）

*   按题号判对错（如 `1对 2错`）；**不**走错题 A/B/C/D 矩阵。
*   **对**：该行 `连续对 +1`；若 **≥2** → `状态=已掌握`，`连续对=0`。
*   **错**：`状态=在练`，`连续对=0`。
*   整卷结束：更新 `dictation_*` 完成日期；md+pdf 从 `未完成/` 移至 `已完成/`（同 §3.4 §5.2）。
*   报告：“✅ 默写批改完成，已更新词表 x 行。”

### 3.6 英语默写词表 (`03_英语默写`)

> 与 `01_错题库` **完全分离**。权威操作说明见 [`萌萌错题记录/03_英语默写/README.md`](萌萌错题记录/03_英语默写/README.md)。

#### 6.0 录入

| 触发 | 数据源 |
|:---|:---|
| **不认识**国名/人名/地名等专名 | `03_英语默写/专有名词词表.md` |
| **短语默写错了** / 中译英短语批改 | `03_英语默写/短语词表.md` |
| **句子默写错了** / 中译英整句批改 | `03_英语默写/句子词表.md` |
| **词性转换默写错了** | `03_英语默写/词性转换词表.md` |

共用列：**错形**｜**状态**｜**`复习次数`**｜**入库日期**｜**年级**｜**备注**｜**`review_level`**｜**`next_review`**（`复习次数`=历次已完成默写卷批改累计；艾宾浩斯见 **§3.5.1**；`连续对` 只读历史）。

*   **禁止**在 `01_错题库` 再建词汇/短语错题。
*   **新词/又错**：按 **§3.5.1** 更新艾宾浩斯字段；**错形**一格多条用 **`；`** 分隔追加。

#### 6.1 复习反馈与组卷

*   **写对/写错**：按 **§3.5.1** 回写词表（`memo_*` 批改或口头反馈均同）。
*   **组卷**：`组默写` / `组默写 短语+专名` → **§3.5.1** `memo_*`。
*   报告：“✅ 已更新英语默写词表：[英文/中文]。”

### 3.7 不熟练知识专区 (`05_不熟练知识专区`)

> 与 `01_错题库` **分工**：错题按**题**；专区按**知识点**合并深度规则。首期：`英语/语法/`（不熟练**语法**专题）；数学/语文子类规则预留。

#### 7.0 物理路径与命名

| 项 | 约定 |
|:---|:---|
| **根目录** | `萌萌错题记录/05_不熟练知识专区/` |
| **首期路径** | `…/英语/语法/{知识点名}_{YYYYMMDD}.md` |
| **毕业** | `…/英语/语法/已掌握/`（专题 `mastery: 4`） |
| **专题 id** | `TOPIC-{科目代码}-{子类缩写}-{YYYYMMDD}-{序号}`（如 `TOPIC-ENG-GRAM-20260608-01`） |
| **原子题链接** | `知识专题: "{basename}"`（无 `.md`） |

#### 7.1 专题 YAML（摘要）

```yaml
id: TOPIC-ENG-GRAM-20260608-01
subject: 英语
子类: 语法
title: "what 与 how 引导的感叹句"
status: 在练          # 在练 | 已掌握
mastery: 1            # 与错题同套 1–4；在练时取关联错题最低 mastery
关联错题: ["ENG-20260607-02"]   # 母题 + 变式档全集；见 7.3 维护
关联变式: []                    # #变式题 / 母题 非空子集
组卷要求: |          # 【组卷指令唯一出处】
  …
最后深化: "2026-06-08"
```

**正文四节**：`## 规则精讲` | `## 口头带练` | `## 关联错题` | `## 变更记录`

#### 7.2 原子错题

* 挂链字段：**`知识专题`**（basename only）；一题最多一个专题。
* **禁止 `组卷备注`**（废止）；需专项考法 → 写专题 **`组卷要求`**。
* §4 保留单题轨迹；专区 `规则精讲` 为合并权威版。
* **权威全集**：凡 YAML 含 `知识专题: "{basename}"` 的错题档，均属该专题；`关联错题` / `关联变式` 为专题侧索引，须与 **7.3** 同步。

#### 7.3 关联维护（挂链 / 变式建档 / 录入时强制执行）

以下任一动作落盘后，**必须**同步更新对应专题 md（及子类 `README.md` 索引）：

| 触发 | 维护动作 |
|:---|:---|
| 录入归档写入 `知识专题` | `关联错题` 追加该 `id`（去重）；`## 关联错题` 表增行 |
| 批改变式判错新建 `#变式题` md | 继承母题 `知识专题`；`关联错题` + `关联变式` 均追加该 `id` |
| `建知识专题` / 手动挂链 | 双向写入；按上表维护两列表 |
| 专题原 `status: 已掌握` 且本次为**新增**关联档 | 执行 **7.4 打回**（仅专题；已毕业错题档不动） |

* **`关联错题`**：该专题下全部挂链错题 `id`（母题 + `#变式题`）。
* **`关联变式`**：其中 `tags` 含 `#变式题` 或 `母题` 非空的子集。
* **`mastery`**：专题取 `关联错题` 对应档的**最低** `mastery`（`status: 在练` 时）。

#### 7.4 专题毕业与打回（仅母亲拍板 · 无系统自动触发）

> **不**在批改后自动检测或提示毕业；是否已掌握**完全由母亲判断**。

**毕业** — 口令：`{知识点简称} 已掌握` / `毕业 {知识点简称}`（如 `what与how 已掌握`）

**第一步 · 扫描（收到毕业口令后必先执行，禁止直接改状态）**

1. 以 `知识专题: "{basename}"` 扫全库（含 `#变式题`）；与专题 `关联错题` 取并集去重。
2. **未毕业档**：`mastery` 非 `4`，或文件不在 `01_错题库/…/已毕业/`。
3. 若存在未毕业档 → **仅反问、不落盘**：
   > 该知识点还有关联错题尚未毕业：
   > | id | 标题 | mastery | 位置 |
   > …
   > 是否确定要**全部毕业**？确认则回 **`全部毕业`**。
4. 若关联错题**均已** `mastery: 4` 且在 `已毕业/` → 直接进入第二步（无需反问）。

**第二步 · 落盘**（母亲回 **`全部毕业`**，或第一步无未毕业档）

1. 专题：`status` → `已掌握`，`mastery` → `4`；md **移入** `…/[子类]/已掌握/`。
2. 凡 `知识专题` 指向该 basename 的错题（含 `#变式题`）：`mastery` → `4`，**移入** `01_错题库/…/已毕业/`（母亲确认覆盖，不等同于组卷逐题验证）。
3. 更新子类 `README.md`（在练 ↔ 已掌握索引）、专题 `## 变更记录`。
4. 报告：「✅ 专题已掌握；关联错题 n 档已同步毕业。」

**打回** — 专题已 `status: 已掌握` 后，**7.3** 因新增关联档触发：

1. 专题：`status` → `在练`；`mastery` → 关联错题最低值；md **移回** `…/[子类]/`（非 `已掌握/`）。
2. **不**批量改动既有已毕业错题档；新档按正常录入/批改规则留在在练。
3. `## 变更记录` 记「新增关联错题，专题打回在练」。

#### 7.5 建档与组卷

| 触发 | 动作 |
|:---|:---|
| `建知识专题 …` | 建专题 md + README 索引 + 关联错题写 `知识专题` + 删除原子题 `组卷备注` + **7.3** |
| `同步 … 专题` | 从关联错题 §4 更新 `规则精讲` + `变更记录` |
| `组 … 专题` | 仅 `status: 在练`；`exam_*`；概念热身摘自专题；按 `组卷要求` 出题 |
| 默认 9+1 抽到挂专题的题 | 读专题 `组卷要求`；清单附摘要；**排除** `status: 已掌握` 专题下的错题 |

**概念热身**：mapping 题有 `知识专题` → 优先从专题 `规则精讲` 提炼；否则沿用 **[概念热身]** 错题 §2 逻辑。

权威 README：[`05_不熟练知识专区/README.md`](萌萌错题记录/05_不熟练知识专区/README.md)
